# 反向管线：从适应症的细胞出发，让数据提名 recorder 与比值

前面 r5–r8 全部是"给定一份候选清单，去验证它"。这一份把方向反过来：
**从适应症的关键细胞拿数据 → 施加可测性门槛 → 用 `r2_pairing` 判据去提名单指标和比值组合
→ 在留出供体上检验提名是否站得住。**

复现：`python3 -m recorder_pipeline.r9_discovery`

---

## 设计

| 环节 | 做法 |
|---|---|
| 适应症 | SLE（唯一一个 Census 里自带双臂、供体数足够的体系） |
| 细胞 | 经典单核细胞 + B 细胞，**45.9 万个细胞** |
| 供体 | **99 健康 + 162 SLE**，供体级 pseudobulk（每个供体每基因一个数） |
| 可测性门槛 | HPA **"Secreted to blood"** 97 个基因，**在看任何效应量之前**施加，所以不可能被反向拟合；另加报告自己的补体候选与 IFN 轴做对照 |
| 判据 | `r2_pairing` 的闭式解，但 ρ 和 κ 这次是**从供体数据测出来的**，不是假设的 |

> **为什么必须用供体级 pseudobulk**：45.9 万个细胞不是 45.9 万个独立单位，是 261 个。
> 在细胞级做统计会得到荒谬的显著性。这里 n=261。

---

## 结果一：判据被真实数据验证了 ★

在 3,486（单核）和 2,701（B 细胞）个基因对上，把测得的 ρ、κ 代进 `r2_pairing` 的闭式解，
与**实测**的 log 比值分离度对比：

| 细胞 | 预测 \|d'\| vs 实测 \|d'\| 的相关 | 二元判断"比值是否胜过分子单独"正确率 |
|---|---|---|
| 经典单核 | **r = 0.9994** | **89.7%** |
| B 细胞 | **r = 0.9996** | **93.1%** |

**只要 ρ 和 κ 是测出来的，闭式解几乎完美重现实测分离度。** 这意味着这个判据可以当作
**筛选器**用：在做任何湿实验之前，用一批供体级数据算出 ρ 和 κ，就能预测哪些比值值得做。

这是对整个框架核心数学的一次大样本经验验证——比 r6 里 Aβ42/40 那个单点验证强得多。

---

## 结果二：但交叉验证后的收益很小 —— 诚实的负面结果

选比值时如果在同一批数据上选又评估，就是我在 `AUDIT.md` 里批评报告 Youden 切点的那种
乐观偏倚。所以这里用**供体级 5 折交叉验证**：在训练折上选最优单指标和最优比值，
在留出折上评估。

| 细胞 | 最优单指标（留出 AUC） | 最优比值（留出 AUC） | 增益 |
|---|---|---|---|
| 经典单核 | 0.855 | **0.883** | **1.13×** |
| B 细胞 | 0.865 | 0.874 | **1.04×** |

**比值只买到 4%–13% 的分离度提升。** 单指标本身已经很强（OAS1 表观 AUC 0.884），
IFN signature 在 SLE 里已经接近这个数据类型的天花板。

> 对项目决策的含义：**在 SLE 血液里另找一个比值组合，不太可能显著超过现有的 IFN 单指标。**
> 这与报告把 SLE 的增量空间放在**载体年龄校正的 C4d**（一个完全不同的模态）而不是
> 放在"再找一个血液标志物"上，是一致的——数据支持报告这个判断。

---

## 结果三：一个限制这类方法的结构性发现 ★

在胜出的比值里（比分子单独强 >20%）：

```
经典单核  1,240 对：62% 是反向轴（L4），  0% 有 ρ>0.5
B 细胞      941 对：66% 是反向轴（L4），  0% 有 ρ>0.5
全部基因对的 |ρ| 中位数 = 0.042
```

**只有反向轴（L4）机制出现，共享噪声（L1）机制完全不出现。**

这**不是**对 L1 的否证，而是这个**数据类型**的性质：

> 单细胞数据的文库大小归一化，已经把 L1 赖以生效的那部分共享技术方差**提前消掉了**。
> 所以 pseudobulk **在构造上就发现不了 L1 配对**。

L1 的共享方差活在**血浆前分析、肾清除、采样时间、个体体质**里——这些在纯化细胞的转录组
里根本不存在。所以：

| 想找哪种配对 | 该用什么数据 |
|---|---|
| **L4 反向轴** | 单细胞/pseudobulk ✓（本管线） |
| **L1 同母体共享噪声** | 只能用**血浆蛋白配对测量**，转录组无效 |

这条限制应该写进报告的"数据库到候选"筛选链——否则会在错误的数据类型上找错误的配对。

---

## 一个必须加的过滤器（否则会被数学骗）

不加限制时，排名最高的"比值"全是**除以一个几乎不表达的基因**：κ 低到 0.03，
分母是单核细胞里的 LEP、VTN、FBLN1、SERPINA3 之类。这些在数学上抬高 d'，
但作为血浆检测**根本测不到**。

加了表达量下限（保留面板第 25 百分位以上，84/112 基因）之后，提名才变得生物学上可读。

> **通则**：比值提名必须同时施加 **表达量下限** 和 **可测性门槛**，否则算法会稳定地
> 推荐"除以噪声"。这一点在报告的 assay_architecture 表里没有写。

---

## 数据提名出来的、值得看的东西

经典单核细胞里，供体级效应量最大的前几名（AUC 为表观值）：

| 基因 | Cohen's d | AUC | 备注 |
|---|---|---|---|
| OAS1 | +1.69 | 0.884 | IFN 轴 |
| IFI6 | +1.57 | 0.867 | IFN 轴 |
| **SIGLEC1** | **+1.57** | **0.867** | **CD169，已有流式检测，蛋白层面可读** |
| ISG15 | +1.51 | 0.858 | IFN 轴 |
| **LGALS3BP** | +1.53 | — | **分泌到血，已有 ELISA** |
| **SERPING1** | +1.52 | — | **C1 抑制物，补体轴 + 分泌到血** |

三个非纯 IFN 的候选值得单独说：

- **SIGLEC1 (CD169)**：单核细胞表面，**已经有临床级流式方案**，与报告主推的
  EC4d/BC4d **同一台仪器、同一管全血**。把它加进同一个 panel 的边际成本接近零。
- **LGALS3BP**：HPA 注明分泌到血，有现成 ELISA。
- **SERPING1 (C1-INH)**：既在补体轴上（与报告的 C4d 主线机制一致），又是血浆蛋白，
  而且它是**抑制物**——按 `RECORDER_FRAMEWORK` 的 R3，抑制物是形成"稳定活性复合物"的
  那一半。**C1s–C1INH 复合物**正是 SLE 版本的 TAT/PIC。这是这轮数据提名里
  **最契合 recorder 框架**的一条线索。[HYP]

---

## 回到"卡在临床转化上的 biomarker，怎么救"

把前面几轮的方法合起来，救援决策其实可以压缩成一条固定顺序的检查表。每一步都能在
**不做新湿实验**的前提下回答，而且每一步都可能直接给出"停"：

| 步 | 问题 | 用什么答 | 停止条件 |
|---|---|---|---|
| 1 | 效应量有多大？ | 已发表中位数，log 尺度 | 倍数 <1.3 且 intended use 就是它 → 改用途或停（C-Alb 死亡预测 AUC≈0.65） |
| 2 | assay 噪声吃掉多少？ | 已发表 CV vs 生物离散度 | 噪声/生物 >0.3 → 先修精密度再谈生物学（PRO-C3 11% CV） |
| 3 | 它到底从哪个细胞来？ | HPA + 单细胞 | 来源广谱 → 分数形式救不了（LCN2 12 种上皮） |
| 4 | 疾病里它在源头稳定吗？ | 配对数据集单细胞 | 跨数据集不可复现 → 砍（NGAL 1.2× vs 144×） |
| 5 | 个体性指数多少？ | 已发表 BV | II<0.6 → **改用个人基线**（19% 的 AKI 事件藏在参考区间里） |
| 6 | 有没有可用的分母？ | 供体级 ρ、κ | ρ<κ/2 → 别做比值；ρ 要在**血浆**上测，不是转录组 |
| 7 | 分母本身带疾病信号吗？ | 单细胞疾病 vs 正常 | 带 → 分母作废（SLE 里 CR1 降 30%） |

七步里有五步能在公开数据上跑完。这就是"救援"最现实的形态：**不是找一个新分子，
而是把同一个分子换一个读出、换一个分母、或者换一个 intended use。**

已经跑过的结论：

| 标志物 | 诊断 | 处置 |
|---|---|---|
| NGAL | 三个层面都不稳定 | **砍**（含我自己提过的单体/总方案） |
| KIM-1 | 源头诱导可复现，尿肌酐归一化有效 | AKI 线改以它为主 |
| C-Alb | 对正常 2.1×，对自己的 intended use 1.31× | **换 intended use**（CKD 3–4 期进展，不是 ESRD 死亡） |
| PRO-C3 | 合成×加工的乘积，非纯合成 | 加一个不依赖加工的胶原 III 对照 |
| uromodulin | 每细胞表达不变、血清跌 4–17× | **提为 CKD 主角**，读作组织质量计 |
| EC4d/BC4d | 分母不能用 CR1 | 用血球仪细胞计数 + 载体年龄 |
| NT-proBNP | II=0.37，指南已用 BMI 分档 | 换连续残差模型，**零新 assay** |

---

## 这一轮没做到的

1. **不是全转录组。** 面板是 HPA "Secreted to blood" 搜索接口返回的 97 个基因 + 15 个
   对照，不是完整分泌组（真实的 secreted-to-blood 集有数百个）。所以是**方法演示与
   局部筛选**，不是全基因组发现。扩到全分泌组是算力问题，不是方法问题。
2. **转录 ≠ 血浆蛋白。** 这些是纯化血细胞里的转录本比值，不是血浆蛋白比值。它们提名的是
   **值得去测蛋白的组合**，本身不是检测方案。逐基因的转录—血浆相关性未验证。
3. **只做了 SLE。** 因为只有它同时满足"双臂 + 供体数足够 + 同一数据集"。肾脏只有 2 个
   配对数据集，动脉粥样硬化 0 个（见 `VALIDATION.md` F）。
4. **没有做 IHC 层面的验证。** HPA 的抗体染色数据可以做蛋白层面确认（补上第 2 条的缺口），
   本轮没做。
5. **CV 只分了供体，没有分中心/批次。** 单中心数据，所以留出估计仍可能乐观。

---

## 参考来源

- SLE 单细胞队列 — CELLxGENE Census 2025-11-08，数据集 `218acb0f-9f2f-4f76-b90b-15a4b7c7f629`（99 健康 + 162 SLE 供体）
- 可测性门槛 — [Human Protein Atlas, secretome "Secreted to blood"](https://www.proteinatlas.org/humanproteome/blood/secretome)
- SIGLEC1/CD169 作为 I 型干扰素流式读出 — [CD169 monocyte flow in autoimmune disease](https://pmc.ncbi.nlm.nih.gov/articles/PMC7093329/)
- C1-INH 与补体复合物 — [SERPING1 / C1 inhibitor](https://www.uniprot.org/uniprotkb/P05155/entry)
