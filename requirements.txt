# Phase 0/1 dependencies (CPU-only, runs in this session).
rdkit>=2024.3
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
requests>=2.28
matplotlib>=3.7
scipy>=1.10

# Added in later phases (heavier; run on GPU/Modal, not this session):
#   chemprop==1.7.0      # Problem A: reproduce LiON D-MPNN (needs its own py3.8 venv)
#   torch                # learned embeddings / DrugCLIP / DiffDock
#   molfeat / molformer  # learned SMILES embeddings
