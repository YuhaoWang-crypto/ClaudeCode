# EvoTwin 扩展：把「相对位置 / 结构 / 遗传图谱」变成可计算的数学对象

对 `EvoTwin 多尺度概率进化模型` 方案的技术性扩展与批评。

原方案的六层骨架（突变供给 → 机制概率 → 细胞状态 → 群体固定 → 系统发育 → 疾病重构）
是对的，但有三处**被符号化却没有被建模**的空白，而这三处恰好就是"相对位置、结构、
遗传图谱"能填进去的地方：

| 原方案 | 空白 | 本文件补什么 |
|---|---|---|
| `U_eff = Σ μ_m·P(functional\|m)` | `P(functional\|m)` 是个占位符 | 用**组合学 + 热力学**把它算出来（M1/M5） |
| 位点被当作二元 present/absent | **坐标本身**没有进模型 | 3'UTR 上的**标记点过程**（M2/M3/M4） |
| 符号表里有 `r`，正文从未使用 | 重组/连锁完全缺席 | **重组–上位性阈值**，给出模块最大跨度（M7） |
| `N_e` 是单一常数 | 群体结构/迁移缺席 | **迁移–选择淹没阈值**与两位点共分化统计（M8） |

所有数字都是 `evotwin/` 里跑出来的，不是断言的：

```bash
pip install numpy scipy
python3 -m evotwin.run_all          # E1-E4：纯理论，无需数据
python3 -m evotwin.e5_real_kmer     # E5：真实 3'UTR 重标定 + 案例基因报告
```

案例部分见 [`BIOMARKER_CASES.md`](BIOMARKER_CASES.md)。

---

## 第一部分 · 九个具体的数学模型

### M1 — 功能突变靶大小：把 `U_eff` 从占位符变成组合量 ✅

原方案说"不能用 `2000×μ`"，但没给替代。正确的对象是**"距目标 k-mer 只差一个碱基的
位置数"**，这是纯组合量：

```
L_eff(k-mer, L) = L · 3k · 4^-k          （单突变邻居落点数）
U_gain          = L_eff · (μ/3)          （只有 1/3 的替换方向命中）
U_loss(单位点)  = k · μ                  （k 个碱基任一被替换即失效）
```

**内部一致性检验（模型正确性的硬约束）**：生灭稳态期望位点数必须等于序列组成期望

```
λ_gain/λ_loss = (L·3k·4^-k/3)/(k) = L·4^-k   ✓ 与 L·4^-k 完全相等（E1 表 1）
```

**计算结果**（μ=1.25e-8, N_e=1e4, L=2 kb, 7mer-m8）：

| s | Θ=2N_eU | P_fix | 等待一条**特定**新边 |
|---|---|---|---|
| 0（中性） | 2.1e-4 | 5.0e-5 | 2,340 百万年 |
| 1e-4 | 2.1e-4 | 2.0e-4 | 575 百万年 |
| 1e-3 | 2.1e-4 | 2.0e-3 | **59 百万年** |
| 1e-2 | 2.1e-4 | 2.0e-2 | 5.9 百万年 |

> ✅ 严格（给定 μ、N_e、随机序列组成近似）。
> **这修正了原方案 §21 toy example 的 `U_eff=1e-6`：真实值低 100 倍（1.07e-8）。**
> 原文"2,500 代 ≈ 62,500 年"应为 ~2.3×10⁶ 代 ≈ 5900 万年，差两个数量级。

**反演出的新数字**：候选边空间 = 300 family × 20k gene = 6×10⁶ 条。在 90 My 窗口内
有益边的实现比例 = 0.78。用 Friedman 2009 的 ~45,000 条保守位点回推：

> **受选择的候选边比例 f_ben ≈ 1%** ⚠️（量级估计，依赖 s=1e-3 与观测计数）

即 99% 的 seed match 是随机噪声——这给了 target prediction 假阳性率一个**从进化第一性
原理导出的先验**，而不是靠经验阈值。

---

### M2 — 位点流动性 Λ_liq：功能保守 ≠ 坐标保守 ✅（本文件的核心）

把某个 miRNA family 在基因 j 的 3'UTR 上的位点集合写成 **[0,L] 上的标记点过程**：

```
出生：强度 λ_gain(x) dx（M1 给出，可再乘结构可及性权重）
死亡：每位点 λ_loss = k
功能读出：R(t) = Σ_k w(x_k)，受稳定化选择 w(n)=exp(-α(n-n*)²)
```

于是"位置中性漂移 + 功能量 R 受 OU 型稳定化选择"。定义无量纲流动性数：

```
        Λ_liq = λ_gain / λ_loss = L · 4^-k
```

它同时是"补偿速度 / 丢失速度"之比：

| UTR 长度 | Λ_liq(8mer) | Λ_liq(7mer) | Λ_liq(6mer) | 7mer 判定 |
|---|---|---|---|---|
| 2 kb | 0.031 | 0.122 | 0.488 | crystalline |
| 10 kb | 0.153 | 0.610 | 2.441 | marginal |
| 20 kb | 0.305 | **1.221** | 4.883 | **liquid** |

> **临界长度 L* = 4^k**：7mer 需要 **16.4 kb**，6mer 只需 **4.1 kb**。

**走过人鼠分歧 d=0.5 之后**（E2 表 3）：

| α | 4N_eα | L | 边保留 | 坐标保留 | **conservation scan 漏检率** |
|---|---|---|---|---|---|
| 1e-5 | 0.4 | 2 kb | 0.193 | 0.054 | 71.9% |
| 1e-5 | 0.4 | 20 kb | 0.727 | 0.032 | **95.7%** |
| 1e-4 | 4.0 | 2 kb | 0.887 | 0.758 | 14.5% |
| 1e-4 | 4.0 | 20 kb | 0.985 | 0.662 | **32.8%** |

> ✅ 严格（CTMC 矩阵指数精确解，非模拟）。

> ⚠️ **上表是均匀 25% 组成 + 等速率突变下的结果。用真实序列重标定后数值有实质变化，
> 见下面的 M2′。上表保留下来是因为它给出解析形式 L\*=4^k；真实数字以 M2′ 为准。**

**两类边由此被严格分开：**
- **site-crystalline edge**（Λ_liq ≪ 1）：丢了补不回来 → 选择只能把**坐标**冻住 →
  现有 conservation scan 能看见。
- **site-liquid edge**（Λ_liq ≳ 1）：位点随时被就地替换 → **坐标不保守，边却保守** →
  现有方法系统性漏掉（真实标定后：P99 为 31%、P99.9 为 38%，见 M2′ 结果四）。

⚠️ **重要方法学推论**：对 liquid 基因，"存在位点"本身不含信息（中性下 α=0、L=20 kb 时
边保留率已经是 0.701）。检验统计量必须换成**位点数相对 Λ_liq 零模型的偏离**，
而不是 presence/absence。这是原方案 §13.3 "追踪 edge 而非坐标"的定量化，
并且给出了它**什么时候必要、什么时候多余**。

---

### M2′ — 用真实 3'UTR 与真实突变谱重标定 Λ_liq ✅

`evotwin/e5_real_kmer.py`。把 M2 的两个理想化假设都换掉：

| 换掉什么 | 换成什么 |
|---|---|
| 25% 等频组成 | Ensembl BioMart 的人类 canonical protein-coding 3'UTR（**10,308 个基因，18.0 Mb**，约占蛋白编码基因的一半；BioMart 限流所致，按整条染色体取，无系统偏倚）；实测组成 A/C/G/T = 0.277/0.211/0.214/0.299（**AU 57.6%**） |
| 等速率突变 | HKY(κ=2) + CpG 超突变(×6)，按全 UTR 平均归一化 |
| 假想 seed | TargetScan `miR_Family_Info` 的 human broadly-conserved family（**108 个**），7mer-m8 位点 = seed+m8 的反向互补 |

**流水线的独立验证**：算出 HMGA2 的 let-7 规范位点数 = **7**（1 个 8mer + 4 个 7mer-m8
+ 4 个 7mer-A1，并集 7），与文献报道的 HMGA2 3'UTR let-7 位点数一致。

#### 结果一：平均而言 Λ_liq 只被低估了 21%，但**尾部被低估了 10 倍**

```
Σ Λ_liq(真实) / Σ Λ_liq(L·4^-7)                      = 1.21
处于 liquid 区 (Λ_liq>1) 的 (基因×family) 对比例      = 0.994%  vs  朴素 0.107%
                                                        →  放大 9.3×
真实 liquid 对的绝对数量 = 11,071 对，覆盖 2,073 个基因
```

> **这是对上一版结论的实质性修正**：site-liquid 不是普遍现象，而是一个
> **~1% 的尾部**（约 2,100 个基因）。朴素模型把这个尾部低估了一个数量级，
> 但也不该被读成"conservation scan 基本失效"。

#### 结果二：临界长度 L\* 不是一个数，而是跨 family 差 93 倍

朴素模型给出单一的 L\*=4⁷=16.4 kb。真实数据给出的是一个谱：

| | family | 7mer-m8 位点 | AU% | CpG | λ_loss | **L\*(kb)** | 全 UTR 出现次数 |
|---|---|---|---|---|---|---|---|
| 最液态 | miR-142-5p | ACTTTAT | 86 | – | 6.25 | **6.9** | 2,348 |
| | miR-203a-3p.2 | CATTTCA | 71 | – | 6.42 | 7.8 | 2,833 |
| | miR-181-5p | TGAATGT | 71 | – | 6.37 | 7.8 | 2,725 |
| 最晶态 | miR-99-5p/100-5p | TACGGGT | 43 | Y | 12.10 | 99.4 | 121 |
| | miR-126-3p.2 | ACGGTAC | 43 | Y | 12.75 | 128.5 | 96 |
| | miR-126-3p.1 | CGGTACG | 29 | Y | 17.71 | **640.1** | 21 |

保守 family 的位点丰度整体只富集 1.24×，但**跨 family 相差 174 倍**（21 → 3,664 次）。

#### 结果三：CpG 把 miRNA family 分成两个演化区制 ✅

| | n | λ_loss 中位 | L\* 中位 |
|---|---|---|---|
| seed 位点含 CpG | 12 | 12.18 | **63.5 kb** |
| 不含 CpG | 96 | 6.68 | 12.7 kb |

CpG family 的位点**丢失快 1.8 倍**，进入 liquid 区需要**长 5 倍**的 3'UTR。
这是一个此前没有被提出的分类：**miR-126 / miR-375 / miR-451 / miR-99-100 这类
CpG-seed family，其调控边在结构上被迫是"晶态"的** —— 位点一旦丢失几乎不可能
就地补偿，所以它们的靶位点必须被选择直接冻在原地。
可检验推论：这些 family 的靶位点应当表现出**最高的跨物种坐标保守性**，
而 miR-142/181/23 这类 AU 富集 family 的靶位点应当坐标保守性最低——
两者的功能重要性却可以相当。

#### 结果四：重标定后的 conservation-scan 漏检率（人鼠 d=0.5，α=1e-4）

| (基因×family) 对分层 | Λ_liq | 边保留 | 坐标保留 | 漏检率 |
|---|---|---|---|---|
| 全部中位数 | 0.050 | 0.833 | 0.766 | **8.1%** |
| P90 | 0.360 | 0.952 | 0.737 | 22.6% |
| P99 | 0.998 | 0.982 | 0.680 | 30.7% |
| P99.9 | 1.838 | 0.990 | 0.614 | 38.0% |
| 最高 1 对 | 6.023 | 0.997 | 0.371 | 62.8% |

> ✅ 计算严格（精确 CTMC + 真实序列计数）。⚠️ α（稳定化选择强度）仍是假设值；
> 漏检率对 α 敏感，表中固定 α=1e-4（4N_eα=4，弱选择区）。

#### 结果五：一半以上的边是"单位点边"

| 规范位点数（8mer ∪ 7mer-m8 ∪ 7mer-A1） | 对数 | 占有位点者 |
|---|---|---|
| ≥1 | 194,644 | 17.5% 的全部对 |
| 恰好 1 | 124,272 | **63.8%** |
| 恰好 2 | 47,005 | 24.1% |
| 恰好 3 | 14,261 | 7.3% |
| ≥4 | 9,106 | 4.7% |

这个分布直接决定了 UTR 变异 biomarker 的可行域，见 `BIOMARKER_CASES.md` 案例 6。

---

### M3 — 位置协同核：间距本身是否受选择 ⚠️

两个 MRE 在 13–35 nt 间距时协同抑制显著超过独立作用（Saetrom/Grimson；人类转录组
在"允许协同的间距"上确有富集）。把它写成核函数：

```
R = Σ_k a(x_k) + Σ_{k<l} c(|x_k - x_l|),   c 峰值在 ~15-26 nt
```

**检验**：在 M2 的中性放置零模型下模拟间距分布，与观测比较。协同距离上的**超额**
= 对**几何**（而非序列）的选择。

**输出的新对象**：`spacing-constrained pair` —— 只有成对才有功能的位点对。预言：
单删任一位点表型被缓冲，双删才崩（一种**顺式的"合成致死"**）。可用 3'UTR 碱基编辑
/ 报告基因直接验证。

---

### M4 — APA 移动边界：位点"可见性"也是一个演化过程 ⚠️

位点 x 只有在所用 polyA 位点下游时才存在。定义暴露度 `e(x, tissue) = P(isoform 覆盖 x)`，
有效边权 `W = Σ_x w(x)·e(x)`。**近端 polyA 位点的坐标本身也在演化**——于是这是一个
**双时间尺度的移动边界问题**：位点在漂移，边界也在漂移，只有内部计数。

**新对象**：`boundary-parked site` —— 紧贴近端 polyA 位点内外的位点。它们是**条件边**，
由 APA 一次性成批开关。肿瘤中普遍的 3'UTR 缩短 ⇒ 可预测**哪些边被批量丢失**，
而不是笼统说"去抑制"。直接可用 TCGA + DaPars PDUI 检验。

---

### M5 — 结构感知的突变→功能核 ⚠️

M1 假设序列随机、位点等价。加上热力学：

```
U_gain = Σ_m μ_m(trinuc context) · σ( β[ ΔG_bind(m) − ΔG_open(x) − θ ] )
```

`ΔG_open` 由 ViennaRNA 局部折叠给出，σ 用 AGO-CLIP 校准。效果是把 Λ_liq 从纯长度量
变成**结构加权的有效长度** `L_eff = ∫ σ(...) dx`，通常显著小于 L（结构封闭区不可用）。

⚠️ 预期：真实 Λ_liq 比 M2 的中性上界低 2–5 倍 → **liquid 区更难达到**，
所以真正的 site-liquid 基因是一个**小而明确的集合**（长 UTR + 高可及性 + AU 富集），
正好适合做优先实验列表。

---

### M6 — 发夹突变距离：proto-miRNA 地形图与"发夹回避" ⚠️

对基因组每个窗口计算 **d_hairpin = 到达一个 Drosha/Dicer 可加工发夹所需的最小突变数**
（打分含茎长、环大小、basal UG / apical UGU / CNNC / mismatched GHG 决定子）。
配合 M1 的 Poisson 供给即得**每个位点诞生一个 miRNA 的等待时间图**。

两个对称的发现方向：
- **miRNA 苗圃（nursery）**：d_hairpin 极小的区域（反向 Alu 对、内含子发夹富集区）→
  预测谱系特异 miRNA 的**诞生热点**。可直接用 MirGeneDB 3.0 的年轻 family 位置做盲检。
- **发夹回避（hairpin desert）**：高表达必需 mRNA 上系统性缺乏"近发夹"序列 →
  对 Drosha 误切的负选择。这是一个**从未被系统扫过的约束类型**。

---

### M7 — 重组–上位性阈值：共依赖模块的最大遗传距离 ✅

对"单独中性/有害、组合才有利"（sign epistasis）的共依赖，稀有 11 单倍型在 00 背景中
每代被重组拆散的速率 ≈ r。稀有时的增长率解析为 `s_ε − r`，数值二分确认：

| s_ε | 数值 r* | 理论 | 最大跨度（1.2 cM/Mb） | 现实对照 |
|---|---|---|---|---|
| 1e-4 | 9.999e-5 | 1e-4 | **8 kb** | **miRNA 簇 "<10 kb" 定义** |
| 1e-3 | 9.990e-4 | 1e-3 | 83 kb | TAD 内 |
| 1e-2 | 9.901e-3 | 1e-2 | 825 kb | 染色体臂局部 |
| 5e-2 | 4.762e-2 | 5e-2 | 3,968 kb | — |

同时还要过漂变关：`4N_e s_ε > 1` ⇒ s_ε > 2.5e-5。

> ✅ 严格（标准两位点配子模型确定性递推；r* = s_ε/(1+s_ε)）。

**三条硬预言：**
1. **跨染色体（r=0.5）的"共进化"边不可能由上位性维持**（需要 s_ε>0.5，不现实）。
   → 数据库里所有跨染色体 miRNA–target 共进化声明，**必须**用各自独立的选择解释。
   这是一条可以立刻拿来做**排除性筛选**的规则。
2. **miRNA 成簇 = 重组回避适应**：经验上的 "<10 kb" 阈值，恰好对应 s_ε ≈ 1e-4 的
   上位性增益。可检验：簇的跨物种破裂率应与**局部重组率**正相关（deCODE/pyrho 图谱）。
3. 共依赖模块应在**重组冷点、倒位、TAD 内部**富集。

---

### M8 — 迁移（gene flow）与两位点共分化 ⚠️

"随时间迁移速度"若取字面的群体迁移 m，代数与 M7 同构：局部适应的耦合模块需要
`s_ε > m` 才不被淹没，且仍需 `r < s_ε`。由此得到一个**新的群体基因组统计量**：

```
共分化协方差  C(i,j) = Cov_pop( f_miRNA-allele , f_target-site-allele ) − 漂变期望
```

用 1000G/HGDP 计算：miRNA 位点等位基因频率与靶位点破坏型 SNP 频率**在人群间的
协变**是否超过共同漂变/群体结构可解释的部分。这是当代人群里**唯一能直接看到
"正在发生的共进化"**的观测量（跨染色体时零模型尤其干净）。

---

### M9 — 带协变量的系统发育 CTMC（统一推断层）✅ 框架 / ⚠️ 结果

把 M2–M8 全部塞进一个可拟合模型：

```
q_ij(edge) = exp( β · z ),
z = { log Λ_liq, 局部重组率 r, ΔG_open, 表达重叠 P_overlap,
      是否同 TAD, miRNA 是否内含子型, 是否簇成员, 靶位点间距协同项 }
```

在系统树上算 presence/absence（或位点计数）的似然，β 就是科学结论本身：
**"哪一个位置/结构/图谱协变量真的改变了边的 gain/loss 速率"**。
与 Pagel–Meade 的 dependent-vs-independent 比较、与 CCM 的 τ=μ×ω 是同一族，
但把 μ 换成了 M1 的可计算 U_eff，把 ω 拆成了可观测协变量。

---

## 第二部分 · 能挖出什么别具一格的生物学

按"证据强度 × 新颖度"排序。

### 1. 液态调控边（site-liquid edge）——一个被低估 10 倍的尾部
长 3'UTR + AU 富集 seed 的边，在功能上被选择维持、在坐标上不保守。
真实数据（M2′）给出的量级是：**11,071 条 (基因×family) 对、覆盖 2,073 个基因**
处于 liquid 区，比朴素模型预测的多 10.3 倍；这些边的 conservation-scan 漏检率
在 P99 是 31%、P99.9 是 38%。
**新现象**：调控约束可以基本不留下坐标保守痕迹。
**怎么找**：位点计数 vs Λ_liq 零模型的 z-score，而非坐标比对。
**为什么以前没人看见**：所有主流方法（TargetScan conserved branch length、PhyloP）
测的都是坐标。
⚠️ **修正**：这不是"大多数边"，而是一个约 1% 的尾部。上一版基于均匀组成模型给出的
"漏检率 96%" 是在近中性 α=1e-5 下取的，不代表典型基因；α=1e-4 下全基因组中位漏检率
只有 8.1%。结论从"主流方法基本失效"降级为"主流方法在一个可枚举的 ~2,100 基因子集上失效"。

### 2. 剂量设定点（repression setpoint）与一个新的剂量敏感度指标
M2 的稳定化选择参数 α 可以逐基因估计——它衡量"总抑制量偏离最优的代价"。
**新对象**：`α_gene` = 独立于 LOEUF/pLI 的剂量敏感度度量，且**有机制含义**
（对 miRNA 抑制总量的敏感度，而非对蛋白编码功能缺失的敏感度）。
**正交验证**：α 应与 gnomAD LOEUF、DepMap 依赖性相关但不重合；不重合的部分
就是"编码序列耐受、但剂量不耐受"的基因——**单倍剂量不足的新候选类**。

### 3. 共依赖的重组半径 —— miRNA 成簇是重组回避
M7 给出 8 kb ↔ s_ε=1e-4 的定量对应，与 miRNA 簇的经验定义重合。
**新解释**：polycistronic miRNA 簇不只是"共转录方便"，而是**上位性模块在有性
重组下唯一可存活的构型**（与超基因/倒位理论同源）。
**可检验**：簇跨度分布应被局部重组率截断；簇的跨物种破裂事件应集中在重组热点。

### 4. miRNA-proof 转录本（回避边）及其在疾病中的破裂
M4 给出统一的零模型，让"保守"和"回避"落在同一张 z 表上。
**新候选类**：跨上百个谱系持续**回避**某 miRNA family 位点的转录本。
**预言**：这类基因应富集剂量关键 / 单倍剂量不足基因（LOEUF 低）。
**biomarker 直接产物**：体细胞 3'UTR 突变或 APA 事件**新建**了一个被长期回避的位点
→ 一个机制清晰、非表达量依赖的驱动候选。
⚠️ M4 同时给出功效边界：**2 kb UTR 的回避几乎不可证**（需要 ~20 个独立谱系），
所以这个扫描只在长 UTR 基因上有效——这本身就是一条有用的方法学限制。

### 5. 边界停泊位点（boundary-parked site）与 APA 开关的边
M4。肿瘤 3'UTR 缩短把成批边一次性关掉。
**新对象**：可枚举的"APA 条件边"清单 + 每个肿瘤样本的**去抑制负荷**分数。

### 6. 间距语法受选择 → 顺式合成致死位点对
M3。单删被缓冲、双删崩溃的位点对。这是 3'UTR 里的**顺式冗余结构**，
和蛋白层的 paralog 冗余是同一逻辑，但从未被系统扫过。

### 7. miRNA 诞生热点 与 发夹沙漠
M6 的双向扫描。前者预测谱系特异 miRNA 在哪出生；后者是一个全新的约束类型
（对 Drosha 误切的负选择）。

### 8. 长 3'UTR = 调控创新天线
M1 的 λ_gain ∝ L 直接推出：3'UTR 越长，单位时间获得新边的速率越高。
**预言**：神经元基因（3'UTR 中位长度远高于全基因组）应表现出**最高的 miRNA 边周转率**，
即"神经系统调控创新快"有一个纯粹的**突变供给几何学**解释，无需诉诸更强的选择。
这是一个可以用现有多物种 UTR 比对直接检验的干净假说。

### 9. 正在发生的共进化（当代人群）
M8 的共分化协方差。这是唯一不依赖跨物种推断的证据线。

---

## 第三部分 · 由此定义的 biomarker 类型

原方案 §20 的七类是概念性的。加上位置/结构/图谱之后，它们变成可计算量：

| 类型 | 计算式 | 数据 |
|---|---|---|
| **Liquidity-aware constraint break** | `z(位点计数 vs Λ_liq)` 高的边在肿瘤中活性反转 | 多物种 UTR + TCGA |
| **Avoidance break** | 长期回避边被体细胞突变/APA 新建 | 多物种 + TCGA 3'UTR 突变 |
| **APA derepression load** | `Σ_edges w·Δe(x)`（PDUI 变化驱动） | DaPars/PDUI |
| **Setpoint deviation** | 观测抑制量 − α 估计的设定点 | 配对 miRNA/mRNA |
| **Spacing-pair biomarker** | 协同位点对的联合状态 | 3'UTR 测序 |
| **Recombination-anchored module** | r < s_ε 窗口内的共依赖模块状态 | 重组图谱 + 共必需性 |

**优先级规则（比原方案更硬）**：优先验证 **M1 判定"难产"（T_wait 长）+ M2 判定
"crystalline"（丢了补不回）+ 疾病中断裂** 的边。三者同时满足意味着这条边
既昂贵又不可修复，破坏它的后果最不可能被缓冲。

---

## 第四部分 · 已知的弱点（必须写在前面）

| 问题 | 影响 | 缓解 |
|---|---|---|
| ~~M1/M2 用随机 25% 序列组成~~ | **已修复**（M2′）：整体只差 1.21×，但 liquid 尾部差 10.3× | 已用 9,392 个真实 3'UTR + HKY+CpG 重标定 |
| M2′ 仍未含结构可及性 | ΔG_open 会压低有效 λ_gain，liquid 区应比 M2′ 更窄 | M5：用 ViennaRNA 加权后重跑 |
| 位点只按 7mer-m8 建模生灭 | 8mer/7mer-A1/非经典位点的并集才是真实功能单元 | 案例表已并列三类计数；生灭 CTMC 待扩展成多类 |
| 星形树功效分析 | M4 的物种数是**下界**（真实树共享内部枝） | 用真实树的有效独立枝数折算 |
| `s` 与 `α` 不可直接观测 | 所有时间尺度结论按比例缩放 | 报告对 s 的敏感性区间，不报单点值 |
| 位点等价假设 | 8mer/7mer/6mer 效力差异被吞掉 | M5 的结构加权 + 分层 k |
| N_e 固定 1e4 | 阈值 `4N_e s>1` 随历史变化 | 用 N_e(t) 轨迹做情景分析 |
| 体细胞 ≠ 种群 | 肿瘤内无跨代固定，M7 的重组论证不适用 | 疾病层单独用克隆动力学模型 |

**不应过度解释**：M7 的"最大跨度"是 sign epistasis 的**必要条件**，不是共进化的证据；
M2 的漏检率是**模型下的**漏检率，需要用已知 CLIP 边做外部标定后才能引用。

---

## 参考

- Saetrom et al. / Grimson et al.：协同位点间距 13–35 nt；人类转录组在协同允许间距上富集
  ([Rinck 2013, RNA Biol](https://www.tandfonline.com/doi/full/10.4161/rna.24955)）
- miRNA 簇定义：同链、间距 <10 kb、共转录
  ([Marco 2013, NAR](https://academic.oup.com/nar/article/41/16/7745/2411007)；
  [Saini 2007, PNAS](https://www.pnas.org/doi/10.1073/pnas.0703890104))
- 原方案引用的 Weissman 2009（适合度谷）、Liu 2023（CCM）、Pagel–Meade 2006、
  Simkin 2020（靶位点 turnover）、MirGeneDB 3.0、miRTarBase 2025 全部保留适用。
