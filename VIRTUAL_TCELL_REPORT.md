# 虚拟 T 细胞 / 免疫细胞：能做什么，不能做什么

**问题**：能否构建虚拟 T 细胞 / 免疫细胞模型，用来替代或预验证
DO11.10 IL-2 分泌、MLR、细胞因子 ELISPOT、FACS 前体频率、供体 HLA 分型
这一类功能性细胞实验？以 pMHC–TCR 为输入表示、以含抗原刺激读数的数据训练，
能否在通路层面对"刺激 vs 静息 T 细胞"做基准验证？

**一句话结论**：五项里只有 HLA 分型能被计算替代，而且仅当供体已有测序数据；
DO11.10 那一类的肽比较有一个可用的**预筛层**（给排序，不给数值），能压缩要做的实验量；
MLR、ELISPOT、前体频率三项没有任何诚实的替代品。通路层面的基准验证是可行的，
但它验证的是 TCR 下游的转录程序，不是 T 细胞的功能输出——这两件事不能互相代替。

本报告中的每个数字要么由本仓库代码算出（标 `[computed]`），要么来自可核查
的一手来源（标 `[source]`）。无法核实的标 `[unverified]`。

---

## 1. 五项实验逐条判定

| 实验 | 它测量的物理量 | 计算替代品 | 判定 |
|---|---|---|---|
| DO11.10 IL-2 分泌 | 一个 TCR 杂交瘤对肽剂量的 IL-2 输出 (pg/mL) | 动力学校对 + ERK 开关模型给**相对排序**与阈值位置 | **部分**：可排序，不可定量 |
| MLR（混合淋巴细胞反应） | 异体反应性 T 细胞群体的增殖 | 无。需要供体重复度 × 前体频率 × 异体肽组，三项都缺 | **不能** |
| 细胞因子 ELISPOT | 每 10⁶ 细胞中分泌某细胞因子的细胞数 | 无。斑点数是频率 × 分泌能力的乘积，两者都不可从序列预测 | **不能** |
| FACS 前体频率 | 抗原特异性 T 细胞在库中的占比 | 无。前体频率由胸腺选择和既往暴露史决定，不是 pMHC 结构的函数 | **不能** |
| 供体 HLA 分型 | 这个人携带哪些 HLA 等位基因 | 从**该供体自己的**测序数据可算（OptiType/HLA-HD 等）；无数据则不可能 | **是（有数据）/ 不能（无数据）** |

HLA 分型那一行值得单独说清楚，因为它经常被混为一谈：从供体的 WES/WGS/RNA-seq
里回推 HLA 基因型是一个已解决的**推断**问题；在没有这个人的任何测序数据时
"预测"他的 HLA 则不是一个建模问题，是一个无解问题。

前一半确实已经做得很好：在独立基准中，OptiType 在全外显子数据上对 MHC-I
四位分型达到约 98% 准确率，arcasHLA 在 RNA-seq 上约 99.4%，HLA-HD 在 MHC-II 上
约 96.2%（Frontiers in Immunology 2022 与 PMC10170851 基准，`[source]`）。
所以如果供体已有测序数据，分型这一项确实不必再做湿实验；如果没有，任何模型都替代不了
采样本身。

---

## 2. 本仓库实现的机制模型（M23）

`grn_pipeline/m23_virtual_tcell.py` —— 一个可运行的最小虚拟 T 细胞：
pMHC 寿命 τ → 动力学校对链 → ERK 正反馈 / SHP-1 负反馈 → 相对 IL-2。
三层各自标注了严格性等级。

### L1 严格层：动力学校对的辨别指数 `[computed]`

McKeithan 校对链（PNAS 1995）的稳态解析式给出

```
d log C_N / d log τ = N·k_off/(k_off + k_p) + 1
```

本仓库用有限差分对闭式解做了验证：

| N（校对步数） | k_off = 1000 | k_off = 10 | k_off = 0.02 |
|---|---|---|---|
| 0 | 0.999 | 0.909 | 0.020 |
| 1 | 1.999 | 1.909 | 1.020 |
| 2 | 2.998 | 2.818 | 1.039 |
| 4 | 4.996 | 4.636 | 1.078 |
| 6 | 6.994 | 6.455 | 1.118 |

两个方向的结论都重要：在快解离区，N=4 的校对链把 2 倍的 pMHC 寿命差异放大成
32 倍的输出差异——这就是 T 细胞抗原辨别力的来源；而在慢解离区，指数塌缩到 1，
校对停止辨别。换句话说，**这个模型对强激动肽之间的区分能力本来就很弱**，
它擅长的是把弱肽和强肽分开。

### L2 拓扑可信、参数仅为示意 `[computed，参数 illustrative]`

加上 Altan-Bonnet & Germain (PLoS Biol 2005) 的反馈拓扑后，ERK 响应变成数字式的，
阈值由配体**质量**而非**数量**设定：

| 配体浓度 L | ERK 开启的 τ 阈值 |
|---|---|
| 0.3 | 1.50 s |
| 1.0 | 1.11 s |
| 3.0 | 0.81 s |
| 10.0 | 0.58 s |

33 倍的配体数量变化只把质量阈值挪动 2.58 倍。这是抗原辨别的核心现象，模型复现了它。
同时在 L=1 时存在 τ ∈ [0.02, 1.03] s 的双稳窗口：同一个配体，静息细胞与已活化细胞
会落在不同的 ERK 状态上。**这正是"刺激 vs 静息"不能当成一个函数来建模的原因**——
细胞的历史是状态变量，不是协变量。

### L3 仅相对量 `[computed]`

一个 τ 从 30 s 到 0.3 s 的改变肽序列（APL）的相对 IL-2 输出是单调的，排序可作为预测；
纵轴是任意单位，**没有任何诚实的办法把它换算成 pg/mL**，除非用它本要替代的那个实验
做标定曲线。

图：`figures/m23_virtual_tcell.png`。

---

## 3. 训练数据的真实状况

这是整个问题的关键。用户设想的路径是"pMHC–TCR 输入表示 + 含抗原刺激读数的训练数据"。
我把 IEDB 的 T 细胞实验全表下载下来直接统计了一遍。

### 3.1 IEDB T 细胞实验表 `[computed]`

来源：`https://www.iedb.org/downloader.php?file_name=doc/tcell_full_v3.zip`，
2026-09-18 下载，解压后 1.35 GB，161 列。

| 统计量 | 值 |
|---|---|
| T 细胞实验记录总数 | 577,789 |
| 不同表位数 | 234,660 |
| **带定量数值的记录** | **5,783（1.0%）** |
| 带 MHC 等位基因级别限制（含 `*`）的记录 | 143,192（24.8%） |
| 唯一 (表位, 等位基因) 对 | 78,427，其中阳性标注 30,336 |
| 记名 TCR 的记录 | 26,127（4.5%） |
| 唯一 (TCR, 表位) 对 | 12,158 |
| 带应答频率(%)的记录 | 294,507（51.0%） |

定性标签分布：Negative 363,314 / Positive 185,429 / Positive-Low 18,087 /
Positive-High 5,826 / Positive-Intermediate 5,133。

实验方法分布：ELISPOT 281,092、³H-thymidine 57,483、ICS 55,240、
biological activity 49,470、ELISA 40,895、multimer/tetramer 35,989、
⁵¹Cr 13,315。读数分布：IFN-γ 释放 309,086、增殖 63,897、定性结合 37,456、
活化 37,185、细胞毒 24,935、IL-5 20,410、**IL-2 释放 16,525**、IL-10 12,621。

### 3.2 致命的那一行

那 5,783 条"定量"记录，按方法拆开是：binding assay 1,951、SPR 1,696、
ELISPOT 861、X 射线晶体学 457、ICS 243、⁵¹Cr 241。单位列 573,651 条为空，
非空的是 nM (2,064)、1/s (827)、M⁻¹s⁻¹ (749)、埃 (458)。

也就是说：**IEDB 里的定量数字几乎全是结合亲和力和结构参数，不是功能输出的量值。**
16,525 条 IL-2 释放记录全部是 Positive/Negative 这类定性标签
（Positive 7,959、Negative 6,997、Positive-Low 1,192、Positive-High 292、
Positive-Intermediate 85），带定量值的只有 35 条，且这 35 条没有单位。

结论直接：**今天不存在能训练"从肽序列预测 IL-2 pg/mL"模型的公开标注数据。**
这不是模型架构问题，是标签不存在的问题。可以训练的是二分类（有/无应答），
这正是现有 T 细胞表位免疫原性预测器在做的事，也正是它们表现平平的原因之一。

另外，数据库里 0 条记录涉及 DO11.10 细胞（950 条涉及杂交瘤），所以连
"用已有数据校准一个 DO11.10 虚拟体"这条退路也没有。

### 3.3 VDJdb：TCR 侧的数据偏斜有多严重 `[computed]`

"pMHC–TCR 输入表示"要有配对的 TCR 数据。我把 VDJdb 源库（GitHub
`antigenomics/vdjdb-db`，master 分支 `chunks/`，2026-09-18 克隆）整个统计了一遍：

| 统计量 | 值 |
|---|---|
| 记录数 | 203,308 |
| **唯一表位数** | **2,118** |
| α 链与 β 链 CDR3 都有的记录 | 101,723（50.0%） |
| MHC-I / MHC-II | 188,017 / 15,291 |

表位覆盖度分布：

| 前 k 个表位 | 占全部记录 |
|---|---|
| 1 | 18.1% |
| 3 | 37.6% |
| 5 | **51.8%** |
| 10 | 67.5% |
| 20 | 77.6% |
| 50 | 87.4% |

单个表位 SLLMWITQV（NY-ESO-1）就占 18.1%，其后是 NLVPMVATV (CMV) 11.0%、
GILGFVFTL (流感) 8.5%、YVLDHLIVV 7.2%、GLCTLVAML (EBV) 7.0%。等位基因侧更极端：
HLA-A\*02:01 一个等位基因占 117,258 条，即 **57.7%**。

这就是为什么 TCR–表位特异性预测模型在未见表位上接近随机：20 万条记录看着很多，
实际的独立表位只有 2,118 个，而且一半的数据来自 5 个免疫优势表位、近六成绑定在
同一个 HLA 等位基因上。模型能学会的是"这条 CDR3 像不像识别 GILGFVFTL 的那一群"，
而不是"CDR3 与肽之间的结合规则"。一个在这种数据上训练出来的 pMHC–TCR 表示，
拿去预测一条新肽的下游功能，其误差来源在第一步就已经确定了。

---

## 4. 单细胞侧：抗原刺激数据有多稀缺

### 4.1 CELLxGENE 全库统计 `[computed]`

来源：`https://api.cellxgene.cziscience.com/curation/v1/datasets`，2026-09-18 拉取。

| 统计量 | 值 |
|---|---|
| 数据集总数 | 2,228 |
| 细胞总数 | 291,753,953 |
| 含 T 细胞/胸腺细胞类型的数据集 | 870 |
| 这些数据集中的细胞数 | 168,073,053 |
| 标题提及刺激/活化/四聚体/肽/表位的 | 4 |
| 放宽到也含疫苗接种/抗原的 | 8 |
| 带 VDJ/TCR 免疫组库测序本体标签的 | 0 |

1.68 亿个 T 细胞，其中被明确标注为刺激实验的数据集是个位数。标题关键词匹配是下界
而非上界（也不覆盖 CELLxGENE 之外的文献），但量级差距不是匹配方法能解释的。
VDJ 那个 0 是关于本体字段的陈述：配对 TCR 数据通常作为 GEO 附件发布，不进入 h5ad 的
assay 槽位。

### 4.2 可用于"刺激 vs 静息"通路基准的核心数据集 `[source]`

| 数据集 | 登录号 | 细胞数 | 供体 | 刺激 | 时间点 | 配对 TCR |
|---|---|---|---|---|---|---|
| Soskic 2022 *Nat Genet* CD4 活化 | EGAD00001008197；处理后数据在 trynkalab.sanger.ac.uk | 655,349 | 119 | anti-CD3/CD28 Dynabeads | 0 h / 16 h / 40 h / 5 d | 无 |
| Cano-Gamez 2020 *Nat Commun* | EGAS00001003215；处理后 BioStudies S-BSST2978 | 43,112 | 4 | anti-CD3/CD28 + Th0/Th1/Th2/Th17/iTreg/IFN-β 极化 | 16 h / 5 d | 无 |
| DICE (Schmiedel 2018) | dbGaP phs001703；dice-database.org | 分选群体（**bulk**） | 91 | anti-CD3/CD28, 4 h（仅 naive CD4/CD8） | 单点 | 无 |
| Immune Dictionary (Cui 2024 *Nature*) | GSE202186；immune-dictionary.org | 386,703（**小鼠**） | 3 只/细胞因子 | 86 种细胞因子皮下注射 | 4 h | 无 |
| Calderon 2019 *Nat Genet* | GSE118189 | bulk ATAC + RNA | 4 | 25 种免疫细胞类型静息 vs 刺激 | — | 无 |
| Schmidt 2022 *Science* CRISPRa Perturb-seq | GSE174292 等 | ~56,000 | 2 | anti-CD3/CD28 + 70 个基因扰动 | 刺激/未刺激 | 无 |

上表及 §4.3–4.5 出现的 GEO 登录号，我用 NCBI E-utilities 逐条查过标题，全部存在且对应
（GSE174292 "CRISPR activation and interference screens decode stimulation responses in
primary human T cells"、GSE202186 "Single-cell RNA-seq dictionary of in vivo immune
responses to cytokines"、GSE118189 Calderon ATAC、GSE200690 "Time-resolved assessment of
single-cell protein secretion by sequencing"、GSE314400 CIPHER-seq、GSE156728 泛癌 T 细胞
图谱、GSE120575 Sade-Feldman、GSE119450 CROP-seq 原代 T 细胞、GSE241783 CD19 CAR-T 图谱）
`[computed]`。EGA / dbGaP 登录号未逐条核验 `[unverified]`。

做通路层面基准验证，Soskic（时间序列深度 + 119 供体）搭配 Cano-Gamez
（细胞因子语境正交）是最合适的组合：两者用同一刺激方式，可比性罕见地好。
注意两者原始数据都在 EGA 受控访问下，处理后矩阵是公开的，而做通路基准用处理后数据即可。

### 4.3 肽身份 × 单细胞响应：几乎空白 `[source]`

| 数据集 | 内容 | 关键限制 |
|---|---|---|
| 10x Genomics 健康供体 CD8 数据集（AN047） | >150,000 CD8 T 细胞，50 个 dCODE 右旋多聚体，5' GEX + VDJ + 抗体捕获 | 测的是**多聚体结合细胞的稳态表型**，不是该肽刺激后的转录响应 |
| Zhang/Satija 2023 *Nat Immunol* | ECCITE-seq 31,396 细胞（RNA+蛋白+TCR），5 种右旋多聚体 | 同上，稳态而非刺激后 |

这就是全部有规模的资源。**不存在把肽身份与单细胞转录响应配对的大型公开数据集**：
上面两个把肽身份和多聚体阳性细胞的表型配起来了，但没有测"这个肽刺激这个细胞之后
转录组怎么变"。用定义好的肽脉冲 APC 再做 scRNA-seq 的实验只以零散小研究存在。
这是一个公开的空白，不是一个检索不充分的结论。

### 4.4 每细胞蛋白质水平细胞因子：约 10% `[source]`

只有两类方法真正把细胞因子**蛋白**测量与同一细胞的转录组连起来：
TRAPS-seq（GSE200690，捕获分泌的 IFN-γ / IL-2 / TNF）和 CIPHER-seq（GSE314400，
胞内细胞因子染色 + 转录组）。

必须区分清楚：CITE-seq 的 173/188/228 抗体板测的是**表面蛋白**（CD69、CD25、PD-1），
是活化标志物，**不是细胞因子分泌**。把它们算作细胞因子蛋白数据是一个常见且严重的误读。

### 4.5 大型扰动图谱不适用 `[source]`

| 图谱 | 有 T 细胞吗 | 有免疫刺激吗 |
|---|---|---|
| Tahoe-100M（1.006 亿细胞，50 个癌细胞系，~1,100 个小分子） | 无原代免疫细胞 | 无，仅小分子 |
| Replogle 2022 全基因组 Perturb-seq（>250 万细胞） | 无（K562、RPE1） | 无 |
| scPerturb（44 个数据集） | 部分有（Datlinger 2017 Jurkat、Shifrut/Marson 2018 原代 T 细胞） | 仅这两个成员数据集有 |

---

## 5. 已有模型到底做到了什么

### 5.1 机制模型这一支：在有限范围内真的定量 `[source]`

这条线才是名副其实的"虚拟 T 细胞"，而且它的数据和代码是公开的：

| 工作 | 做到了什么 | 读数 |
|---|---|---|
| McKeithan 1995 *PNAS* | 动力学校对的原始模型，解析解释辨别力锐化 | 无细胞因子 |
| Altan-Bonnet & Germain 2005 *PLoS Biol* | 近端信号 ODE + 正负反馈 → **数字式 pERK** 开关，解释拮抗 | **pERK，不是 IL-2**（常被误引） |
| François 等 2013 *PNAS* | 自适应动力学校对，辨别力对剂量稳健 | 拮抗现象 |
| Lever 等 2016 *PNAS* | 11 个 pMHC 配体、SPR 实测亲和力、10⁶ 倍亲和力×剂量范围；解释钟形剂量响应 | IFN-γ / MIP-1β / IL-8，**不是 IL-2** |
| **Achar 等 2022 *Science*** | 机器人平台测 IL-2/IFN-γ/TNF/IL-6/IL-17A 时间序列；60 维压到 2 维潜空间再压到 4 参数"弹道"轨迹，最终近似单参数 v₀；v₀ 与 EC50 跨 10⁸–10¹¹ 相关，且**对抗原数量在 3 个数量级内不变**；定义 6 个不重叠的抗原类别 | **五种细胞因子的完整时间曲线，可生成** |
| Achar/Bourassa 等 2025 *Cell* | 修正的 AKPR 模型经 MCMC 拟合 TCR/CAR 拮抗数据，指导设计出特异性更好的双 TCR/CAR T 细胞 | 细胞因子分泌 |

Achar 2022 的数据与代码已发布（GitHub `frbourassa/antigen_encoding_theory`，
Zenodo 10.5281/zenodo.5759168）。这意味着**本报告 §2 的 M23 模型不是终点，而是起点**：
存在一个可对接的真实数据集，可以把 M23 的 τ→输出映射换成对真实细胞因子轨迹的拟合。

**但这条线的输入是实测的效力（EC50 或 k_off），不是肽序列。** 没有任何
sequence→EC50 模块。所以"肽序列进、IL-2 pg/mL 出"端到端**无人解决**，
该领域自己的综述（Bourassa 等，*Annu Rev Biophys*，arXiv:2511.18626）也是这么写的：
"未来工作需要确立 TCR 信号体如何从抗原输入显式计算出这些潜变量"。

### 5.2 转录组基础模型这一支：还没赢过均值基线 `[source]`

这一点必须讲清楚，因为它直接决定"virtual cell 模型能否做这些验证"的答案。

- Kedzierska 等 *Genome Biology* 2025：在细胞类型注释与批次整合上，**直接选高变基因
  (HVG) 就胜过 scGPT 和 Geneformer**；Geneformer 的嵌入批次方差比原始数据还大；
  scGPT 的掩码重建还不如"预测均值"这个朴素基线。
- Ahlmann-Eltze、Huber & Anders *Nature Methods* 2025：在双扰动预测上
  **所有模型误差都高于可加基线**；在遗传互作上**没有一个模型赢过"不变"基线**；
  在未见扰动上"没有一个深度模型能稳定胜过均值预测或线性模型"。诊断性的一句：
  scGPT、UCE、scBERT 对多数基因的预测**在不同扰动之间几乎不变**。
- Csendes 等 *BMC Genomics* 2025，Pearson-delta：

  | 模型 | Adamson | Norman | Replogle-K562 | Replogle-RPE1 |
  |---|---|---|---|---|
  | 训练集均值（基线） | 0.711 | 0.557 | 0.373 | 0.628 |
  | scGPT | 0.641 | 0.554 | 0.327 | 0.596 |
  | scFoundation | 0.552 | 0.459 | 0.269 | 0.471 |
  | 随机森林 + GO 特征 | **0.739** | **0.586** | **0.480** | **0.648** |

- Arc Institute 的 Virtual Cell Challenge 2025（>1,200 支队伍提交）官方结语原文：
  "扰动预测模型尚未在所有指标上稳定优于朴素基线"，且"几乎所有模型在 MAE 上都比基线更差"。
  冠军队自己也说"纯 AI 方法没有稳定胜过统计基线"，因而把训练集差异基因频率和均值表达
  硬编码成特征；季军是 Wilcoxon 检验 + 伪bulk 这样的经典统计方法，还在 PDS 榜首。

**与本问题最直接相关的一条负面结果**：在一个 2,200 万细胞的原代人 CD4⁺ T 细胞数据集上
（约 12,000 个基因敲低、4 供体、3 个活化时间点），模型的语境内判别指标中位数 >0.9，
但**只召回了约 9% 的真实差异基因**（bioRxiv 2026.02.04.703804，`[unverified]` 预印本）。
判别指标高而差异基因召回低，正是 §5.3 要警惕的那个陷阱。

### 5.3 因此对用户三个设想的直接回答

1. **"pMHC–TCR 的输入表示"**——可以构建，但 TCR 特异性预测器在**未见表位**上
   接近随机。八个模型（ERGO、ERGO-II、NetTCR-2.0/2.2、TITAN、PanPep、SCEPTR、EPACT）
   的独立评测结论是：它们"隐式记住了表位特异的特征，而不是学到可泛化的结合规则"
   （arXiv:2606.04994，`[unverified]` 预印本）。已见表位上 AUC₀.₁ 约 0.5–0.67。
2. **"训练数据含抗原刺激读数"**——§3 已用 IEDB 全表证明：读数是定性的。
   能训练的是二分类。已有的 IL-2 肽分类器（IL2pred、IL2Pepscan）正是这样：
   IL2Pepscan 外部验证准确率 79.88%、MCC 0.6，作者自己写明"缺乏对预测出的
   IL-2 诱导肽的实验验证"。它们不含 MHC/TCR 语境，也不输出浓度。
3. **"用它验证肽在科学上结合下游的"**——结合与下游是两件事。上面第 1 条说明结合
   本身在未见表位上就不可靠；即使结合预测准确，也没有模型把结合强度映射到信号强度。

---

## 6. 通路层面基准验证：唯一站得住的那条路

用户的第三点——"若后续产生刺激 vs 静息 T 细胞的 scRNA-seq，可在通路层面做基准，
验证的是 TCR 下游生物学"——这个判断是对的，而且是本报告中唯一不需要打折扣的一条。

原因在于它换掉了被预测的量。它不再要求模型从肽序列预测一个功能输出，而是要求模型
在给定"已被刺激"这个条件下，复现 TCR 下游的转录程序（NF-κB、NFAT、AP-1、MAPK、
IL2RA/IL2/IFNG 的诱导、代谢重编程）。这个量在 §4.2 的数据集里有大样本量测量，
可以做留一供体、留一时间点的检验。

但必须写清楚它**验证了什么、没验证什么**：

- 验证了：模型是否学到了 T 细胞活化的转录程序。
- 没验证：模型能否区分两条不同的肽。刺激 vs 静息的差异是巨大的、共同的；
  两个都能激活的肽之间的差异是细微的、肽特异的。前者做得好，对后者不构成证据。

这一点直接连到 §5.2 的基准教训：在扰动预测里，一个只预测"平均扰动效应"的
基线常常能赢过深度模型，因为共同响应占了方差的绝大部分。刺激 vs 静息基准非常容易
掉进同一个陷阱，所以**必须同时报告一个"预测所有细胞的平均刺激响应"的基线**，
否则这个基准的分数无法解读。

---

## 7. 建议的做法

如果目标是减少功能实验的次数而不是取消它们：

1. **用呈递预测做第一层筛选。** NetMHCpan 类的呈递预测是目前单项最强信号，这是
   本仓库 `neoantigen-selection` 技能在 TESLA 真实数据上得到的结论，而且它自带的
   复合评分并没有赢过单用 NetMHCpan。第一层只筛掉"不可能被呈递"的肽。
2. **用 M23 这类机制模型做第二层排序**，输出的是相对排序和阈值位置，不是数值。
   它的用途是决定实验里的肽浓度梯度该覆盖哪个区间。
3. **功能读数照做。** DO11.10 IL-2、ELISPOT、MLR、前体频率这四项没有替代路径，
   §3.2 说明了为什么：训练它们所需的标签在公开数据里不存在。
4. **HLA 分型从供体测序数据算**，不要"预测"：OptiType / arcasHLA / HLA-HD 已经够用。
5. **如果要真的推进这件事，有两个现成的落点。** 一是 Achar 等 2022 *Science* 的
   细胞因子时间序列数据与代码已公开（GitHub `frbourassa/antigen_encoding_theory`，
   Zenodo 10.5281/zenodo.5759168），可以直接把 §2 的 M23 从示意参数换成对真实
   IL-2/IFN-γ/TNF/IL-6/IL-17A 轨迹的拟合——这是把"虚拟 T 细胞"从玩具变成工具的
   最短路径。二是 Schmidt 等 2022 *Science*（GSE174292，已核验）在原代人 T 细胞里
   做了全基因组 CRISPRa/i 筛选，**以 IL-2 和 IFN-γ 产量为选择读数**，还有阵列式
   分泌组数据；据我检索，尚无公开模型用它训练"扰动 → 细胞因子分泌"预测器。
   这是目前最明显的、没人动过的训练集。
6. **通路基准单独做，并且带上平均响应基线**，报告的是 TCR 下游生物学的保真度，
   不要外推成肽区分能力。

---

## 8. 复现

```bash
pip install numpy scipy matplotlib
python3 -m grn_pipeline.m23_virtual_tcell     # L1/L2/L3 全部数字 + 图
```

§3、§4.1 的统计脚本是一次性流式统计，来源 URL 和取数日期已在正文标注。
