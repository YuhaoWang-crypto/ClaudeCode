# Core analysis stack
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
requests>=2.28

# Imaging and radiomics.  pyradiomics needs a two-step install: its build
# backend imports versioneer and numpy outside PEP 517 isolation.
#   pip install SimpleITK versioneer
#   pip install --no-build-isolation pyradiomics
SimpleITK>=2.2
versioneer>=0.28
pyradiomics>=3.0

# Optional
matplotlib>=3.7
pytest>=7.0
