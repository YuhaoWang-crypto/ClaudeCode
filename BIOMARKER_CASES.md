# 七类 EvoTwin biomarker 的具体案例

每一类都用同一套结构写：**分子事实 → 用哪个 EvoTwin 量刻画 → 算出来的数 →
biomarker 定义式与阴性对照 → 可证伪预言 → 已知的坑**。

数值来自 `evotwin/`（`python3 -m evotwin.run_all`），序列来自 Ensembl BioMart 的
全部人类 canonical protein-coding 3'UTR，位点定义来自 TargetScan `miR_Family_Info`
的 human broadly-conserved family（seed+m8 的反向互补 = 7mer-m8 位点）。

选案例的原则：**优先选已经有明确分子机制、且模型能给出与现有方法不同判断的**。
其中两个是**故意选的失败案例**（rs61764370、SLITRK1 var321）——一个框架如果不能
解释已知的阴性结果，它就没有资格解释阳性结果。

## 案例基因的计算结果（全部来自 `evotwin/e5_real_kmer.py`）

位点数按 TargetScan 三类规范位点计（8mer / 7mer-m8 / 7mer-A1 / 并集）；
Λ_liq 与保留率按 7mer-m8 类的生灭 CTMC 算，人鼠分歧 d=0.5、α=1e-4。

| 基因 × family | UTR(bp) | 8mer | 7m8 | 7A1 | **并集** | Λ_liq | 边保留 | 坐标保留 | 漏检率 | 判定 |
|---|---|---|---|---|---|---|---|---|---|---|
| **HMGA2** × let-7 | 2,999 | 1 | 4 | 4 | **7** | 0.262 | 0.936 | 0.735 | 21.4% | crystalline |
| **IGF2BP1** × let-7 | 6,701 | 3 | 4 | 4 | **5** | 0.397 | 0.956 | 0.723 | 24.4% | marginal |
| **LIN28B** × let-7 | 4,549 | 2 | 4 | 3 | **5** | 0.287 | 0.941 | 0.733 | 22.1% | crystalline |
| **KRAS** × let-7 | 4,549 | 0 | 0 | 0 | **0** | 0.147 | 0.899 | 0.746 | 17.0% | crystalline |
| MYC × let-7 | 1,993 | 0 | 0 | 1 | 1 | 0.389 | 0.955 | 0.724 | 24.2% | marginal |
| CCND1 × miR-15/16 | 3,192 | 1 | 1 | 2 | 2 | 0.691 | 0.974 | 0.719 | 26.2% | marginal |
| BCL2 × miR-15/16 | 5,279 | 1 | 1 | 1 | 1 | 0.200 | 0.921 | 0.762 | 17.2% | crystalline |
| E2F1 × miR-17/20 | 1,255 | 0 | 2 | 0 | 2 | 0.202 | 0.920 | 0.748 | 18.7% | crystalline |
| CDKN1A × miR-17/20 | 1,532 | 0 | 2 | 0 | 2 | 0.056 | 0.838 | 0.762 | 9.1% | crystalline |
| SIRT1 × miR-34a | 1,797 | 0 | 0 | 2 | 2 | 0.107 | 0.875 | 0.746 | 14.8% | crystalline |
| **SOCS1** × miR-155 | 426 | 1 | 1 | 1 | **1** | 0.000 | 0.787 | 0.787 | 0.0% | crystalline |
| **INPP5D** × miR-155 | 1,195 | 1 | 1 | 1 | **1** | 0.031 | 0.824 | 0.785 | 4.8% | crystalline |
| **GAPDH** × miR-125b | **201** | 0 | 0 | 0 | **0** | 0.000 | 0.776 | 0.776 | 0.0% | crystalline |

**这张表的第一个用处是验证流水线本身**：HMGA2 的 let-7 位点并集算出来是 **7**，
与文献报道的 HMGA2 3'UTR let-7 位点数一致；SOCS1/INPP5D(SHIP1) 各 1 个 8mer
（经典 miR-155 靶点）；E2F1/CDKN1A 各 2 个 miR-17/20 位点；SIRT1 2 个 miR-34a 位点。
这些都不是拟合出来的，是从序列直接数出来的。

---

## 案例 1 · Evolutionary Constraint Break —— let-7 → HMGA2

### 分子事实
HMGA2 的 3'UTR 携带多个 let-7 互补位点（文献报道至少 5–7 个预测位点），let-7 对
HMGA2 的抑制**依赖多个位点共同作用**。在脂肪瘤/平滑肌瘤等良性肿瘤中，染色体易位
断点聚集在 HMGA2 第 3 内含子，产生只含外显子 1–3、**整段丢掉 3'UTR** 的截短转录本；
let-7 只能抑制全长 mRNA，不能抑制截短型。这是"长期稳定的调控边被一次性物理切除"
最干净的例子。

关键旁证：Wang 2009 发现**单独删除 LCS1（let-7 位点 1）并不足以**造成 HMGA2 上调。
这正是多位点冗余的直接实验证据——也正是下面模型要量化的东西。

### 用哪个 EvoTwin 量
不是"位点是否保守"，而是 **重建成本 T_rebuild**：

```
T_rebuild(g, f) = n_sites / λ_gain(f, g)        （单位：中性分歧度 d）
```

λ_gain 来自 E5（真实 UTR k-mer 组成 + HKY+CpG 突变谱）。一条边越"难产"，
它在疾病中被破坏时的信息量越大。

### biomarker 定义

```
CBS(g, f) = log10( T_rebuild(g,f) ) × ΔR(g,f)
ΔR = 正常组织 → 肿瘤 的有效抑制量变化（位点数 × 暴露度 × miRNA 丰度）
```

**阴性对照**：按 3'UTR 长度、GC、表达量、突变负荷匹配的基因集；以及**同一基因的
非保守 family**（同一段 UTR、同样的测量噪声，但没有演化约束）——这是最强的内部对照，
因为它把所有技术性混杂都控住了。

### 算出来的数
HMGA2 3'UTR = 2,999 bp，let-7 规范位点 **7 个**，Λ_liq = 0.262（**crystalline**：
位点丢了补不回来）。判定为 crystalline 是关键——它意味着这 7 个位点是**演化上昂贵、
不可再生**的资产，而易位一次性把它们全部切除。

对照 IGF2BP1（同为 let-7 靶点、3'UTR 6.7 kb、5 个位点、Λ_liq=0.397 marginal）：
它在癌中的机制是 **3'UTR 缩短**而非易位截短，与它更长、更接近 liquid 区一致。

### 可证伪预言
1. 在 TCGA 中，3'UTR 丢失/截短事件应当在 **T_rebuild 高 + crystalline** 的基因上富集，
   且这种富集在控制了表达量与 UTR 长度之后仍然存在。
2. 单位点破坏的效应应当 ∝ 1/n_sites（见案例 6）。HMGA2 有 7 个位点 →
   **预测单位点 SNV 的效应量只有 11%**（模型算出 ΔR/R = 0.113），
   低于绝大多数实验的检出下限——与 Wang 2009「删 LCS1 不足以造成上调」一致。
3. 反之，只有 1 个位点且 Λ_liq≈0 的边（SOCS1×miR-155，ΔR/R = **1.00**），
   单位点破坏就应当有满量程效应。

### 坑
- HMGA2 3'UTR 截短同时移除了 let-7 之外的 RBP 位点与 APA 信号，
  单靠关联无法把因果归给 let-7；必须用位点定点突变的等位对照。
- "7 个位点"是预测位点数，不同资源计数不同；本文用统一的 7mer-m8 口径重新计数。

---

## 案例 2 · APA Escape Panel —— CFIm25/NUDT21 低表达肿瘤

### 分子事实
CFIm25（NUDT21 编码）是 APA 的主调控因子。它在胶质母细胞瘤中下调 → 全局 3'UTR
缩短 → 一批癌基因蛋白升高而 mRNA 变化不大（Masamha 2014 Nature；后续工作把
Pak1 的 APA 归到 Nudt21）。这不是"某条边断了"，而是**一整批条件边被同时关掉**。

### 用哪个 EvoTwin 量
M4 的**暴露度**与**边界停泊位点**：

```
e(x, 样本) = P(所用 isoform 覆盖位置 x) ≈ PDUI
有效边权 W(g,f) = Σ_x w(x)·e(x)
S_APA(g) = { 落在近端 polyA 与远端 polyA 之间的保守位点集合 }
```

只有 S_APA 里的位点是 **APA 条件边**；近端 UTR 内的位点不受 APA 影响，是天然对照。

### biomarker 定义（三层面板）

```
APA-Derepression Load(样本) = Σ_g  |S_APA(g)| · (1 − PDUI_g) · [miRNA_f 在该组织活跃]
面板读出 = { miRNA 丰度, PDUI(UTR isoform), 蛋白/mRNA 比 } 三元组
```

**为什么必须是三元组**：单看 mRNA 看不到（APA 不改变总 mRNA 量多少）；
单看蛋白看不到机制；单看 PDUI 不知道哪条边被打开。三者合起来才构成机制性证据链。

### 可证伪预言
1. 在 CPTAC 中，**蛋白/mRNA 残差**应当与 |S_APA| × (1−PDUI) 正相关，
   而与近端 UTR 内的位点数不相关（这是关键的内部特异性检验）。
2. NUDT21 敲低实验中，升高的应当优先是 |S_APA| 大的基因。
3. 该负荷分数应当独立于总突变负荷预测预后。

### 坑
- PDUI（DaPars 等）估计噪声大，短读长数据尤其；需要配对正常样本。
- 3'UTR 缩短同时丢掉 RBP/m6A 位点，miRNA 不是唯一解释。
- 肿瘤纯度会系统性偏移 PDUI，必须校正。

---

## 案例 3 · Target-Avoidance Break —— GAPDH 3'UTR 体细胞突变新生 miR-125b 位点

### 分子事实
卵巢癌中存在一个高频的 GAPDH 3'UTR 体细胞突变，**创造出一个 miR-125b 结合位点**
（Yang 等, 2020）。这与常见方向相反：不是破坏已有位点，而是**在一个本不该有位点的
地方造出一个位点**。PCAWG 也在 FOXA1、ALB、SFTPB、NFKBIZ、TMEM107、TOB1 的 3'UTR
发现了复现性体细胞事件，其中多为 indel。

### 用哪个 EvoTwin 量
E4 的回避似然比。零模型不是"随机序列"，而是 **E5 给出的真实 λ_gain/λ_loss 生灭稳态**：

```
H0: 位点按中性生灭出现     H1: 获得位点受负选择 (s_avoid > 0)
Avoidance z = 观测位点数相对 Poisson(Λ_liq) 的偏离，在系统树上按 CTMC 计算
AB(g,f) = Avoidance_z(g,f) × 1[体细胞事件在该 UTR 新建了 f 的位点]
```

### 为什么这类候选值得单独一栏
它把"非编码体细胞突变"从**统计学复现性**问题变成**机制性**问题：
一个突变如果落在一个跨上百个谱系被系统性回避的位置上，它的先验就完全不同于
一个落在中性区的突变。这对 3'UTR 这种复现性极低、样本量永远不够的区域尤其重要。

### 可证伪预言
1. 高 Avoidance z 的基因应当富集低 LOEUF / 单倍剂量不足基因（gnomAD 正交验证）。
2. 新建位点的体细胞事件应当在高 Avoidance z 的基因上过表达，
   且其转录本水平变化方向应与该 miRNA 的活性一致。
3. 该类事件应当集中在 miRNA 高表达的组织（否则新建位点无功能后果）。

### 算出来的数：一个**否定性**结论，而且很有用
GAPDH 的 canonical 3'UTR 只有 **201 bp**，miR-125b 规范位点 **0 个**，
**Λ_liq = 0.000** —— 中性期望位点数本来就约等于零。

> **所以：观测到 0 个 miR-125b 位点在 GAPDH 上，携带的信息量约等于 0。**
> 这个突变**确实创造了**一个位点（文献层面的事实），但**不能**由此宣称
> "这个位点是被演化系统性回避的"。E4 的功效分析给出同样结论：2 kb 的 UTR
> 就需要约 20 个独立谱系，201 bp 的 UTR 在任何可得物种数下都无法拒绝零假设。

这正是这套框架该起的作用之一：**把"机制上真实"和"演化上可证"分开**。
GAPDH 案例应当被归类为 **gain-of-site 驱动事件**（案例 3 的机制），
但**不能**被归类为 avoidance break。

真正适合做 avoidance 扫描的，是 **UTR 长 + Λ_liq 高**的基因——
按 M2′ 的分布，这大约是 2,073 个基因构成的子集。扫描应当**先按 Λ_liq 排序再做**，
而不是全基因组一把梭。

### 坑（E4 已经量化过）
- **短 UTR 的回避不可证**（上面 GAPDH 就是实例）。
- 星形树功效分析是上界，真实系统树共享内部枝，要再打折。
- 注释缺失会被误读为回避。
- CpG-seed family（miR-126/375/451/99-100）的位点本来就稀有（全 UTR 仅出现 21–195 次），
  它们的"缺失"更没有信息量——这类 family 必须单独设阈值。

---

## 案例 4 · Edge Reversal —— let-7 轴的三种"失效/反向"，读出各不相同

### 分子事实
同一条 let-7 边可以以三种完全不同的机制"反向"，而**现有的差异表达分析把它们混为一谈**：

| 机制 | 分子事件 | 特征读出 |
|---|---|---|
| 加工层失效 | LIN28A/B 阻断 pri/pre-let-7 加工 | **pri-let-7 ↑，mature let-7 ↓** |
| 竞争层失效 | HMGA2 等高丰度转录本作为 ceRNA/诱饵吸走 let-7 | 诱饵转录本 ↑，其他靶点去抑制，mature let-7 不变 |
| 效应层反向 | AU-rich 元件语境下 miRNA 转为**激活**（Vasudevan 2007 的 miR-369-3） | 相关系数**变号** |

### biomarker 定义

```
ERI(g,f) = ρ_disease(miRNA_f, target_g) − ρ_normal(miRNA_f, target_g)
只在通过演化过滤的边上计算：T_rebuild 高 且 跨物种保留
机制判别 = { pri/mature 比, 诱饵转录本丰度, ρ 的符号 } 三维分类
```

**演化过滤是关键**：不加过滤地做"相关性翻转扫描"会得到成千上万条噪声边；
只在"难产 + 长期保留"的边上做，才把假设检验的负担降到可控。

### 可证伪预言
1. 反向事件应当集中在**位点数多、miRNA 丰度高**的边（可被 sponge 的边）。
2. 加工层失效应当在 pri/mature 比上留下痕迹，而竞争层失效不应当——
   这两个亚型应当在同一队列中可分离。
3. 效应层真正的符号反转应当罕见，且与 AU-rich 元件共定位。

### 坑
- ceRNA 效应的化学计量条件苛刻（需要诱饵丰度与 miRNA 丰度可比），
  很多报道的 sponge 在生理浓度下不成立——必须报告绝对拷贝数而不是相对表达。
- 相关系数翻转极易被细胞组成变化（肿瘤 vs 免疫浸润）伪造，必须做去卷积。

---

## 案例 5 · Motif Biomarker —— MYC/E2F–miR-17~92 与 p53–miR-34a–SIRT1

### 分子事实
- **MYC/E2F/miR-17~92**：MYC 与 E2F 诱导 miR-17-92，而 miR-17/20 又负调 E2F，
  构成前馈 + 反馈复合结构（Aguda 2008 PNAS）。MIR17HG 在淋巴瘤中扩增。
- **p53–miR-34a**：p53 直接诱导 miR-34a，miR-34a 抑制 SIRT1/MYC 等，SIRT1 又
  去乙酰化 p53 —— 一个完整的负反馈。p53 突变型肿瘤中整条回路状态翻转。

### 用哪个 EvoTwin 量
M9 的 8 状态 CTMC：三条边 (TF→miRNA, TF→mRNA, miRNA⊣mRNA) 的存在与否 = 8 个状态。
biomarker **不是任何单个节点**，而是**回路的状态向量**。

```
motif state = ( sign & 显著性 of TF→miRNA, TF→mRNA, miRNA⊣mRNA )
Motif Score = 观测状态与"正常组织中该 motif 的模态状态"的距离
```

### 可证伪预言（这一条现在就能用公开数据检验）
> **FFL/FBL 中的边应当比孤立边具有更长的 T_residence 与更强的跨物种保留。**

做法：用 ENCODE ChIP-seq 定义 TF→miRNA / TF→mRNA，用 TargetScan+CLIP 定义
miRNA⊣mRNA，把边分成"在 motif 内"与"孤立"两组，比较跨物种位点保留率
（并按 Λ_liq、UTR 长度、表达量匹配）。这是一个**当天就能跑**的检验。

### 临床含义与坑
miR-34a 模拟物 MRX34 的 I 期试验因免疫相关严重不良事件终止。
这恰恰说明：把 miRNA 当**药**用，和把 motif 状态当**分层 biomarker** 用，
是两件事。后者的价值不依赖于前者成功——但试验设计里如果没有"回路是否完整"
（p53 状态、SIRT1 水平）的入组分层，阳性信号会被稀释掉。

---

## 案例 6 · Variant-aware Biomarker —— 一个预测中的失败 + 一个结构案例

### 6a. rs61764370（KRAS LCS6）：模型预测它**应该**失败

**事实**：rs61764370 位于 KRAS 3'UTR 的一个 let-7 互补位点（LCS6）。早期报道它与
卵巢癌/乳腺癌风险、抗 EGFR 疗效相关。随后：
- 一项跨 OCAC/BCAC/CIMBA、合计 **140,012 名女性**的研究报告**卵巢癌 OR=0.99
  (p=0.74)、乳腺癌 OR=0.98 (p=0.19)**，即无关联；
- 结直肠癌预后的系统综述与 meta 分析同样未发现显著关联。

**模型为什么预测失败——算出来的数比预想的更强**：

> KRAS 的 3'UTR 长 4,549 bp，但**规范 let-7 位点数 = 0**
> （8mer 0 个、7mer-m8 0 个、7mer-A1 0 个）。

也就是说，KRAS 的 LCS1–LCS10 是**非经典 let-7 互补位点**，不是种子匹配位点。
rs61764370 所在的 LCS6 因此是一个**非经典位点上的变异**：它的效应必须完全经由
弱的非经典结合传递。模型据此预测其效应量在群体尺度上不可检出——
与 140,012 人的零结果一致。

一般化成一条可写下来的定律：

```
单位点变异的效应量  ∝  1/n_sites  ×  1/(1 + Λ_liq)
```

模型对本案例基因算出的预测效应量（`e5_real_kmer.py` 第 [6] 节）：

| 基因 × family | n_sites | Λ_liq | **预测 ΔR/R** | 判定 |
|---|---|---|---|---|
| SOCS1 × miR-155 | 1 | 0.000 | **1.000** | 可检测 |
| INPP5D × miR-155 | 1 | 0.031 | 0.970 | 可检测 |
| BCL2 × miR-15/16 | 1 | 0.200 | 0.833 | 可检测 |
| CDKN1A × miR-17/20 | 2 | 0.057 | 0.473 | 可检测 |
| E2F1 × miR-17/20 | 2 | 0.202 | 0.416 | 可检测 |
| CCND1 × miR-15/16 | 2 | 0.690 | 0.296 | 边缘 |
| LIN28B × let-7 | 5 | 0.288 | 0.155 | **预测检测不到** |
| IGF2BP1 × let-7 | 5 | 0.397 | 0.143 | **预测检测不到** |
| **HMGA2 × let-7** | 7 | 0.263 | **0.113** | **预测检测不到** |
| **KRAS × let-7** | **0** | 0.148 | **0.000** | **无规范位点 → 预测无效应** |

同一条定律同时解释了两个已知事实：**KRAS rs61764370 的关联失败**，
以及 **HMGA2 删 LCS1 无效**。它们不是两个孤立的意外，是同一个冗余缓冲量的两次体现。

**全基因组的可行域**（M2′ 结果五）：17.5% 的 (基因×family) 对有 ≥1 个规范位点，
其中 **63.8% 只有 1 个位点**。这 124,272 条单位点边才是 UTR 变异 biomarker
应该去找的地方；多位点边（HMGA2/IGF2BP1 型）对单变异免疫，只对整段 UTR 丢失敏感。

**SLITRK1 var321** 是同一类：它把 miR-189 位点上的 G:U 摆动配对换成 A:U
Watson–Crick，属于**增强已有位点**而非新建；后续大规模家系样本未能重复该关联。

> 这两个案例的价值在于：它们把"UTR 变异 biomarker"从**逐个碰运气**变成
> **有先验排序的假设检验**——先算 n_sites 与 Λ_liq，再决定值不值得做队列。

### 6b. rs2910164（pre-miR-146a）：结构侧的案例
该变异位于 pre-miR-146a 茎区，改变发夹稳定性从而影响 Drosha/Dicer 加工效率
（M5/M6 的 P_processing 项）。读出量是 **ΔΔG(hairpin)** 与 **pri/pre/mature 比**，
而不是结合亲和力。这与 6a 是两条不同的机制通道，必须分开建模。

### biomarker 定义

```
VAB(变异) = ΔW_predicted × 先验可检测性
ΔW = 结合侧: ΔΔG_bind + ΔΔG_open 的位点权重变化   （靶侧变异）
     加工侧: ΔΔG_hairpin → ΔP_processing            （miRNA 侧变异）
先验可检测性 = 1/n_sites × 1/(1+Λ_liq) × [该组织 miRNA 活跃]
```

### 现成的验证数据集
已有对癌基因 3'UTR 变异的大规模并行报告基因筛选（MPRA, Nat Commun 2024），
直接可以检验上面那条"效应量 ∝ 1/n_sites × 1/(1+Λ_liq)"的定律：
把 MPRA 测得的效应量对模型预测做回归，**斜率与 R² 就是该定律的判决**。

### 坑
- 群体分层与多重检验：早期 3'UTR 变异关联研究普遍样本量不足。
- 报告基因构建体脱离原生 UTR 语境（缺少远端位点、缺少 APA），
  会系统性**高估**单位点效应——这正好是 6a 中体内失败的原因之一。

---

## 案例 7 · Network Residual Score —— 用正常组织训练，用残差找异常

### 思路
前六类都是"找某条边坏了"。第七类反过来：**建一个正常网络的前向预测模型，
把预测不了的部分定义为 biomarker**。

```
在正常组织(GTEx / CPTAC 正常)上拟合：
    protein_g  ~  f( mRNA_g , Σ_f w(g,f)·miRNA_f , TF 活性 , PDUI_g )
在肿瘤样本上计算残差：
    NRS_g = ( observed − predicted ) / σ_g
按演化信息量加权聚合：
    NRS(样本) = Σ_g  ω_g · |NRS_g| ,   ω_g = log10 T_rebuild(g) 的归一化
```

### 为什么加演化权重
不加权的残差分数会被高噪声、高丰度基因主导。用 T_rebuild 加权，等于
**优先相信那些"难产 + 长期保留"的边上的异常**——这是整个 EvoTwin 框架
在 biomarker 层唯一真正的增量。

### 可证伪预言
1. NRS 应当与 DepMap 依赖性相关，而与 mRNA fold-change 的相关性显著更弱
   （否则它只是差异表达的换皮）。
2. 去掉演化权重后，NRS 的预测力应当**下降**——这是权重有效性的直接检验。
3. NRS 高的基因应当富集案例 1–3 里识别出的具体机制事件（断裂/APA/新生位点），
   即七类之间应当**相互印证**而不是各说各话。

### 坑
- 循环论证风险最高的一类：训练与评估必须严格分离样本与基因集。
- 蛋白组覆盖有限（CPTAC 约数千蛋白），残差只能在覆盖到的基因上算。
- 肿瘤纯度与细胞组成是最大的混杂，必须去卷积后再算残差。

---

## 七类之间的关系与优先级

它们不是并列的七个想法，而是**同一个量在不同层面的投影**：

```
                      T_rebuild（难产程度） × Λ_liq（可补偿程度）
                                    │
        ┌───────────┬───────────────┼───────────────┬───────────┐
     边断裂        边被 APA        边新生         边反向      整体残差
     (案例1)        关掉(案例2)     (案例3)       (案例4)     (案例7)
                                    │
                        回路层(案例5)   变异层(案例6)
```

**建议的执行顺序**（按"能用公开数据当天开跑"排序）：

| 顺序 | 做什么 | 需要的数据 | 判决标准 |
|---|---|---|---|
| 1 | 案例 5 的预言：motif 内的边是否更保守 | TargetScan + ENCODE + 多物种 UTR | 匹配后仍显著 |
| 2 | 案例 6 的定律：效应量 ∝ 1/n_sites/(1+Λ_liq) | MPRA (Nat Commun 2024) | 回归斜率与 R² |
| 3 | 案例 2 的特异性检验：蛋白残差 ~ 远端位点数 | CPTAC + DaPars | 远端 vs 近端位点的对比 |
| 4 | 案例 3 的正交验证：Avoidance z vs LOEUF | 多物种 UTR + gnomAD | 相关性与方向 |
| 5 | 案例 1/4/7 | TCGA + CPTAC | 需要更多建模假设，放最后 |

前两项**不需要任何肿瘤数据**，且能证伪整个框架的核心假设。如果它们不成立，
后面的五类都不必做——这是这份案例集最重要的一条。
