# 细胞系虚拟化蓝图

虚拟细胞不是一个模型，是七层不同输入／不同输出的模型叠在一起。这份文档按层拆开：
每层吃什么、吐什么、拿什么公开数据训、在哪一步会骗你；然后回答第二个问题——
用今天的公开库，能不能给 K562、RPE1、MCF7 这类细胞系做出一个能替一部分实验的替身，
以及那个替身应该按什么指标结算。

配套仓库：`grn_pipeline`（本文档中的 L6 机理动力学层）。

---

## 0. 结论先行

**能建，但建出来的不是"一个端到端虚拟细胞"。** 可交付的东西是：一张特定细胞系的
状态卡，加上五到六个各自可单独证伪的预测模块，加上一个敢返回"这个问题我表示不了"
的路由器。

**它的价值不是取代实验，是把筛选从"全测"变成"排序后测"。** 所以验收指标不是
Pearson 相关系数，是**富集倍数**和**每个真阳性的实验成本**——这两个数才谈得上
和湿实验预算对账。

**最关键的一条设计约束来自之前几轮的实测：跨细胞系外推目前是架构受限，不是数据
受限。** 所以一期只做*系内*虚拟细胞——挑那些自己就有全基因组扰动数据的细胞系
（K562、RPE1），绕开外推这个已知会塌的环节。想靠"多买几条细胞系"把它救回来的路，
已经在六条系的语料上测过并且被否掉了（§3.6、§3.7）。

---

## 1. 七层模型地图：什么进、什么出

"虚拟细胞"这个词底下混着七类完全不同的模型。它们的输入不同、输出不同、训练语料
不同、失效方式也不同，把它们当成一件事讨论是这个领域最大的沟通成本。

"可虚拟化程度"：**可替代** = 干湿两边同精度；**可预筛** = 只能排序、仍需实验确认；
**仅研究** = 还不到决策级。

| 层 | 名称 | 代表模型 | 输入 | 输出 | 程度 |
|---|---|---|---|---|---|
| **L0** | 身份层 / 细胞状态卡 | CCLE、DepMap Omics、ENCODE、Cellosaurus | 细胞系名 | 基线表达谱、突变、拷贝数、融合、甲基化、染色质可及性、倍性、STR 身份 | 可替代 |
| **L1** | 顺式序列层（cis） | Enformer、Borzoi、AlphaGenome、Sei、Evo2（编码区 LoF） | DNA 窗口（ref vs alt），最长 ~1 Mb + 组织/细胞类型 track | **该位点自身**的表达、剪接、ATAC/DNase、CAGE、接触图变化 | 可预筛 |
| **L2** | 表征层 / 单细胞基础模型 | scGPT、Geneformer、scBERT、scFoundation、UCE、CellPLM、scPRINT、C2S-Scale、TranscriptFormer | 单细胞表达向量（基因 token 化 / 分箱） | 细胞 embedding → 类型注释、批次整合、零样本迁移、微调底座 | 可预筛 |
| **L3** | 反式扰动响应层（trans） | scGen、CPA、GEARS、PerturbNet、CellOT、STATE、Stack | 细胞基线状态 + 扰动身份（基因 KD/KO/OE、化合物+剂量） | 扰动后的**全转录组位移**、DE 基因集、通路方向与幅度 | 可预筛 |
| **L4** | 形态 / 高内涵影像层 | CellFlux、IMPA、PhenDiff、MorphDiff｜表征侧 CellProfiler、DINO4Cells | **同一批次**对照孔图像 + 扰动条件 | 扰动后 Cell Painting 图像 → FID/KID、MoA 分类、状态插值 | 可预筛 |
| **L5** | 药敏 / 终点层 | DeepCDR、PaccMann、DrugCell、MOLI、Precily、tCNNS | 细胞系多组学 + 化合物结构 | IC50 / AUC / 存活率 / 协同性打分 | 可预筛 |
| **L6** | 机理动力学层 | Human-GEM、Recon3D、Yeast-GEM（FBA/FSEOF）、BioModels ODE、全细胞模型｜本仓库 `grn_pipeline` | 化学计量矩阵 + 动力学参数 + 培养基交换通量 + 基因缺失 | 稳态通量、生长率、必需性、双稳/滞回、分岔点、临界慢化早期预警 | 可预筛 |
| **L7** | 路由 / 智能体层 | virtual-yeast-agent 式 router + verifier（Qian et al., Nature 2026） | 自然语言机理问题 | 路由 + 四值判定：`supported` / `contradicted` / `not_representable` / `inconclusive` | 仅研究 |

### 三个最容易混的边界

1. **L1 只回答"这个编辑会不会让*这个位点自己*闭嘴"**，它对反式后果完全失明；
   **L3 只回答"假设它已经闭嘴了，*其他几千个基因*怎么动"**，它对序列完全失明。
2. **L2 的 embedding 不是 L3 的预测。** 把基础模型的表征拿来当扰动预测用，需要额外证据。
3. **L6 的 FBA 给的是稳态通量，给不了浓度、速率、滴度或时间曲线。** 这是定义性质，
   不是实现缺陷。

---

## 2. 公开数据盘点：哪条细胞系"喂得饱"

建一个细胞系的虚拟替身，瓶颈几乎从来不是模型，是这条系上有没有**成对的扰动前后数据**。

`***` 全基因组级／核心系　`**` 有覆盖但不完整　`*` 稀疏或无。
规模数字为量级，正式立项前应对当期 release 核对。

| 细胞系 | 来源 | Perturb-seq | L1000 | Cell Painting | DepMap CRISPR | CCLE 组学 | ENCODE 表观 | 药敏 |
|---|---|---|---|---|---|---|---|---|
| **K562** | CML，女性 | `***` 全基因组 | `**` | `*` | `***` | `***` | `***` Tier 1 | `***` |
| **RPE1** | 视网膜色素上皮，近二倍体非癌 | `***` 必需基因集 | `*` | `*` | `**` | `**` | `*` | `*` |
| **MCF7** | 乳腺癌 ER+ | `*` | `***` 核心 | `**` BBBC021 | `***` | `***` | `***` Tier 1 | `***` |
| **A549** | 肺腺癌 | `*` | `***` 核心 | `***` JUMP | `***` | `***` | `***` Tier 1 | `***` |
| **U2OS** | 骨肉瘤 | `*` | `*` | `***` JUMP 主力 | `**` | `**` | `*` | `**` |
| **HepG2** | 肝癌，肝毒性代理 | `**` | `***` 核心 | `*` | `***` | `***` | `***` Tier 1 | `***` |
| **HCT116** | 结肠癌，MSI | `**` | `**` | `*` | `***` | `***` | `**` | `***` |
| **Jurkat** | T 细胞白血病，免疫代理 | `**` | `*` | `*` | `**` | `**` | `**` | `**` |
| **HeLa / PC3 / A375** | L1000 常驻 | `*` | `***` | `*` | `***` | `***` | `**` | `***` |

### 2.1 主要数据源，以及每个能给虚拟细胞什么

| 资源 | 模态 | 量级 | 在虚拟细胞里的位置 |
|---|---|---|---|
| DepMap / CCLE | CRISPR KO 适应度 + 多组学 | ~1,100 系 KO ／ ~1,800 系 组学 | L0 状态卡 + L5 必需性真值 |
| Replogle Perturb-seq | CRISPRi 单细胞转录组 | K562 全基因组 ~9.8k 靶／RPE1 必需集 ~2k 靶 | **L3 的唯一系内全基因组语料** |
| LINCS L1000 / CMap | 1,000 landmark 基因 bulk 签名 | ~10⁶ 签名，~80 系，~2 万扰动 | L3/L5 化合物侧 + 最强的检索 baseline |
| JUMP Cell Painting | 5 通道形态图像 | ~10⁵ 化合物 + ~10⁴ 基因扰动 | L4 训练与评测 |
| BBBC021 / RxRx1 | 形态图像，带 MoA 标签 | MCF7 113 化合物 12 MoA／HUVEC ~1.1k siRNA | L4 标准 benchmark 与 OOD 划分 |
| GDSC / PRISM | 剂量-响应存活 | ~1,000 系 × ~500 药／条码池化 ~500 系 × ~4.5k 药 | L5 真值 |
| Tahoe-100M | 化合物 × 细胞系 单细胞 | ~10⁸ 细胞，~1,100 药 × ~50 系 | L3 化合物侧的新语料，目前最有希望的增量 |
| scPerturb | 统一化的扰动单细胞集合 | 数十个数据集 | 跨数据集评测的统一坐标 |
| ENCODE | ATAC/DNase/ChIP/Hi-C/CAGE | Tier 1 系全套 | L1 的细胞系 track + L0 染色质状态 |
| Human-GEM / BioModels | 化学计量模型 / 曲线拟合 ODE | ~13k 反应／~1k 条精选模型 | L6 机理骨架 |

**一个反直觉的读法。** 表里模态最全的那几条（MCF7、A549、HepG2）恰恰**没有**系内全
基因组扰动数据；有全基因组扰动数据的那两条（K562、RPE1）在化合物和形态侧几乎是空的。
这就是为什么 §4 把一期拆成**两条互补的线**——基因扰动线走 K562/RPE1，化合物表型线走
MCF7/A549/U2OS——而不是指望一条系包打全场。

---

## 3. 实测能力边界

这一节是全文的地基。下面 12 条不是文献综述，是之前几轮在真实数据上跑出来的结果——
其中多条会把"直觉上应该 work"的方案直接否掉。任何虚拟细胞方案如果绕过了这些结论，
基本可以判定还没开始就已经错了。

### 3.1 ❌ 跨细胞系外推是*架构*受限，不是*表征*受限

把留出真值构造成"上下文特征"喂给模型（oracle 臂），跨系轴上的表现**比完全不给上下文
特征还差**。这意味着换更好的基因表征／基础模型救不了这条轴。必须把两条迁移轴——未见
靶点（表征能帮）和未见细胞系（架构问题）——**分开报**；合并成一个数字会把"一条开着、
一条堵死"掩盖掉。

### 3.2 ❌ CRISPRi 训练的模型迁移到 CRISPRa 等于掷骰子

在 Norman 2019 CRISPRa 上用 CRISPRi 训练的源模型测：相关系数 `r = 0.009`，判别力
`0.511`——和随机不可区分。敲除数据迁移稍好但仍严重退化（约为同模态一致性的 30%）。
**永远不要把敲入、过表达或激活类问题路由给敲低训练的模型**；这类问题只能走 L1 顺式层。

### 3.3 ❌ 敲低"深度"不携带下游"幅度"的信息

Replogle/Nadig CRISPRi（4 条系、1,249 基因、4,996 观测）：同基因跨上下文的耦合
`r = +0.055`；跨扰动是 `−0.09 ~ −0.17`；深敲 vs 浅敲的下游幅度中位数只差 `1.22×`。

**这是生物学结论不是建模瑕疵**：网络在意的是"是哪个基因"，不是"掉了多少"。所以把
L1 预测的表达降幅当作"剂量"串给 L3 的链式方案没有信号可传——这条链要断开。

### 3.4 ❌ 当前跨系模型几乎*无视*它的上下文输入

留出 HepG2、261 个敲低：真实对照谱 Pearson δ `0.350`；加 sd=1.0 噪声 `0.349`；换成
不含任何查询系信息的源系平均 `0.344`；换成**错误细胞系**的谱 `0.317`。用真实谱和用
源系平均做出的预测互相关到 `0.99`。所以"给它一个更好的细胞系表征"这条改进路径，
在当前架构下买不到东西。

### 3.5 ❌ 序列模型给不了细胞系身份

序列-到-功能模型预测**表达水平**很好（154 个上下文指纹基因上 Spearman `+0.641`），
预测**跨细胞系差异**基本为零（`+0.024`，对各自表达水平做残差化后 `+0.035`）。机制
是结构性的：所有细胞系共用一套基因组，细胞系特异性只能从 track/本体头出来——而对角线
检验没通过（三条系里只有一条自己的 track 是最佳匹配，对角均值 0.498 vs 非对角 0.418）。
**把组织 track 当成细胞系身份用是错的。**

### 3.6 ⚠️ "多买几条细胞系"的收益会饱和

四条 CRISPRi 系上的共识（平均）臂：k=1/2/3 得 `0.163 / 0.230 / 0.278`，1/k 渐近线
`0.324`；外推到六条系约 `0.296`，比三条只多 `+0.018`（真值尺度为 1.0）。**但不能只看
这一条就下结论**——共识渐近线只约束"平均"这种做法，不约束拟合斜率的架构。所以必须
同时跑斜率臂再判断。

### 3.7 ❌ 斜率臂也救不回来——而且差距是*扩大*的

在六条系语料（236,606 细胞，51 个共享靶点）上直接测：斜率模型在 k=2,3,4,5 全输
（`0.0103/0.0139/0.0157/0.0165` vs 共识 `0.0174/0.0208/0.0236/0.0257`），且差距随 k
**扩大**而非收窄。最保守子集上结论相同。这是函数形式错了，不是样本量不够。下一步应该
换成跨扰动共享统计强度的**低秩 上下文×响应 分解**，而不是继续给每个扰动单独拟合。

### 3.8 ❌ 系统性偏差可复现、可解释、但*不可校正*

癌系签名迁到别的谱系会产生可复现的方向性偏差（复制/MCM 与胆固醇程序高估、p53 低估，
24 个程序里 15 个在五条目标系上符号一致）。把它减掉**四折全输**（shape 0.339→0.307，
direction 0.745→0.718，error 0.961→1.028）。原因在一个诊断里：偏差向量与真实跨扰动
均值相关 `−0.972`——所谓"偏差"本质上就是你自己的共识项从反面看。**做任何偏差校正之前
先跑这个相关。**

### 3.9 ⚠️ 单指标提升可能在记分卡上净为负

先验驱动的收缩把归一化绝对误差改善了 `0.0119`（bootstrap CI 不含零，是真的），但判别力
掉了 `0.0178`——因为判别力奖励的是**幅度离散度**，而收缩按定义在消灭离散度。六指标无权
平均下净值 `−0.001/指标`，扫描收缩强度后最优解是"完全不收缩"。**上任何修正之前，在目标
记分卡的每个通道上都打分。**

### 3.10 ⚠️ 先验的排序质量 ≠ 下游精度

先验与标签的秩相关从 `+0.583` 提到 `+0.681`，绝对误差只动了 `0.00005`（CI 触零）；提到
`+0.840` 才动了 `0.0017`。而且整条先验路线本身有界：把归一化误差从 `0.964` 推到 `0.948`
——96% 误差里的 1.6 个点；相比之下把形状精度提到 Pearson 0.89 能推到 `0.578`。
**永远带 oracle 臂**，否则"先验已饱和"和"先验很弱"分不开。

### 3.11 ⚠️ 基础模型零样本常常打不过 HVG+PCA

多个独立评测里，单细胞基础模型的零样本 embedding 在细胞类型聚类和批次整合上**等于或
低于**调好的 scVI，甚至低于 HVG+PCA；微调通常有用，零样本经常没用。预训练语料严重偏向
人、健康、10x、热门组织。基因词表是固定的——嵌入前必须查基因重叠率，否则大量基因会被
静默丢弃。同理，L3 的深度扰动预测器在 2024 年的评测里**没有打赢"训练集平均位移"这个
基线**。

### 3.12 ⚠️ "零样本"通常名不副实——污染是集合求交，不是推断

公共档案拼起来的预训练语料是按 accession 一个文件存的，所以污染审计是确定性的集合运算：
拿 BioProject 的 SRX 列表和语料清单求交即可。常见判定是**「预训练污染、标签干净」**——
模型见过被扰动细胞的表达状态，但没见过每个细胞受了哪个扰动。这**不是零样本**，且必须写进
每一张表的标题和图例，不能塞进脚注。GEM 侧同理要诚实：酵母 GEM 的必需性 F1 只有
`0.49–0.54`，且 83% 的基因根本不可表示。

### 压成一句设计约束

今天可以建的虚拟细胞，是*在有自有扰动数据的细胞系内部*做插值和排序的系统；一旦要求它
外推到新细胞系、新扰动模态（激活/过表达）、或者输出绝对量（IC50、浓度、时间曲线），
已知就会失败。方案必须把这三件事挡在门外，而不是寄希望于它们碰巧成立。

---

## 4. 蓝图：具体建什么

### 4.1 四条设计原则

1. **系内训练，不跨系外推。** 一期只选自己有全基因组扰动语料的细胞系。跨系那条轴
   （§3.1）先不碰。
2. **各层并列输出，不串联当剂量。** 顺式和反式两个答案**并排给**，各自带各自的有效域，
   中间不发明剂量映射（§3.3）。顺式层的正确用法是**可行性闸门**：先判断这个编辑在这条
   系里到底能不能把靶点关掉——这是反式层结构上看不见的失败模式。
3. **每个数字带基线和噪声天花板。** 没有基线阶梯的预测数字不进报告。每个分数同时报
   "占噪声天花板的百分比"——90% 的指标已经没有余量，40% 的那个才是没建模的生物学所在。
4. **路由器必须能说"不"。** 四值判定里 `not_representable` 是**特性不是缺陷**。一个
   什么都敢答的虚拟细胞在药物筛选里是负资产。

### 4.2 一期细胞系选型

| 细胞系 | 角色 | 为什么是它 | 喂饱哪几层 |
|---|---|---|---|
| **K562** | 基因扰动主力 | 唯一同时有全基因组 Perturb-seq、ENCODE Tier 1 全套表观、DepMap 与 CCLE 深度组学的系 | L0 L1 L2 L3 L6 |
| **RPE1** | 非癌近二倍体对照 | 核型干净，检验"结论是不是癌系特有伪影"的对照。必需基因集 Perturb-seq 可用 | L0 L3 |
| **MCF7 + A549** | 化合物表型主力 | L1000 核心系、GDSC/PRISM 药敏最深、ENCODE Tier 1。化合物→转录组→存活这条链最完整 | L0 L3(化合物) L5 |
| **U2OS** | 形态读出 | JUMP Cell Painting 主力系，L4 的训练与评测都在这里 | L0 L4 |
| **HepG2** | 选配：肝毒性 | 肝毒性代理 + ENCODE Tier 1 + L1000 核心。想做安全性预筛时加进来 | L0 L1 L5 |

### 4.3 模块清单：每个模块的验收合同

关键在最后两列。**一个模块如果没写清楚"必须打赢谁"，它就没法被证伪，也就不该上线。**

| 模块 | 输入 | 输出 | 训练/查询数据 | 验收指标 | 必须打赢的基线 |
|---|---|---|---|---|---|
| **M0 状态卡** | 细胞系名 | 表达/突变/CNV/可及性/倍性 | CCLE + ENCODE | 与官方 release 逐字段一致 | —（查库，不建模） |
| **M1 顺式闸门** | 编辑坐标 + ref/alt | 该位点表达/剪接/ATAC 变化 | AlphaGenome/Borzoi，该系 track | 启动子编辑上 CAGE>ATAC>RNA 且剪接头≈0；±40bp 位置对照 | 同类变异的位置置换对照 |
| **M2 系内扰动** | 该系基线 + 敲低靶点 | 全转录组位移 + DE 集 | K562/RPE1 Perturb-seq | DE 基因上的方向与幅度；分层报未见靶点 | **训练集平均位移**、最近邻检索、线性加性 |
| **M3 化合物-表型检索** | 化合物结构 或 L1000 签名 | 最相似的已测扰动 + MoA 归类 | L1000 + JUMP + BBBC021 | 留出化合物上的 **MoA top-k 富集倍数** | 结构指纹最近邻（这个基线很强） |
| **M4 形态生成** | 同批对照孔图 + 扰动 | 扰动后 Cell Painting 图 → MoA | JUMP / BBBC021（8 化合物 OOD 集留出） | FID/KID + OOD 集 MoA 准确率 | 同批对照孔直接复制（identity 映射） |
| **M5 药敏排序** | M0 组学 + 化合物结构 | 存活率/AUC *排序*（不是绝对 IC50） | GDSC + PRISM | 系内 Spearman + top-k 富集 | 该化合物的全系中位数、该系的全药中位数 |
| **M6 动力学读出** | M2 的通路位移 | 双稳/滞回/分岔距离/临界慢化预警 | BioModels 精选模型 + `grn_pipeline` | 方差与 lag-1 自相关在阈值处峰值（已有阳性对照） | 随机置换的扰动标签 |
| **M7 路由器** | 自然语言问题 | 路由 + 四值判定 + 数值审计 | 上述模块注册为工具 | `not_representable` 召回率；未溯源数字标 `[UNVERIFIED]` | 直接问 LLM（无工具） |

**M2 的一个具体架构建议。** §3.7 已经否掉了"给每个扰动单独拟合斜率"这条路。既然一期
是系内，主力应该是**共识 + 靶点表征残差**：共识项吃掉大部分方差（它是那条难打的基线），
表征只负责解释残差里的未见靶点部分。跨系那部分等有了低秩 上下文×响应 分解再说。

---

## 5. 哪些"检测"真的能虚拟化

按今天的能力分三档。分档的依据是 §3，不是乐观程度。

### ✅ 可替代 — 干湿同精度，可以直接省掉实验

注意这一档里大部分其实**不是"建模"，是"查库"**——这本身就是最容易被忽略的效率来源。

- 靶点在该系的表达水平、拷贝数、突变状态、是否有融合（CCLE 直接查）
- 该靶点在该系是否为必需基因（DepMap CRISPR 直接查，比任何模型都准）
- 化合物的结构警报：PAINS / Brenk / NIH 干扰子筛除，理化性质与类药性
- sgRNA 的全基因组脱靶枚举与 CFD 特异性打分（Cas-OFFinder 是穷举，不是预测）
- 细胞系身份与污染核对（STR / SNP 指纹）

### 🟡 可预筛 — 只能排序，仍需实验确认，但能大幅减量

- **化合物的转录响应方向**：L1000 检索基线本身就很强，模型必须先打赢它
- **MoA 归类**：Cell Painting 形态签名 + 检索，是这一档里成熟度最高的
- **单基因敲低的下游通路**：仅限该系有 Perturb-seq 覆盖的靶点及其邻域
- **化合物-靶点结合排序**：可用共折叠打分，但要选对指标——本仓库 M10 实测 `opt_score`
  与实测活性 ρ≈+0.6，而 `binding_confidence` 是 ρ≈−0.2，选错指标等于反着排
- **组合筛选的优先级**：把 n² 的组合空间排序后取头部去测
- **启动子/剪接变异的顺式效应**：L1 的本职工作，但必须带位置置换对照
- **网络失稳预警**：临界慢化指标（方差、lag-1 自相关）在分岔点附近上升，已有阳性对照

### ❌ 不可替代 — 现在做出来的数字不该进决策

- **外推到没有扰动数据的新细胞系**（§3.1、§3.4）
- **CRISPRa / 过表达 / 敲入的后果**——用敲低训练的模型做这件事等于掷骰子（§3.2）
- **绝对 IC50 与完整剂量-响应曲线**；只能排序，不能给数
- **时间动力学、代谢物浓度、滴度**——FBA 的定义就给不了这些
- **从细胞系外推到患者响应**；这些模型的有效域是"被测读出在被测条件下"
- **毒性放行决策**；结构警报和 Tox21 模型是筛选辅助，不是监管级评估

---

## 6. 收益怎么结算

报 Pearson 相关系数没法跟湿实验预算对账。要报的是两个数：**富集倍数**和**每个真阳性的
实验成本**。下面是一个示意算例——数字要换成你自己项目的命中率和单孔成本。

```
初筛库容量                        10,000
真实命中率（历史值）              0.5%  → 50 个真阳性
虚拟预筛后取头部                  500   (5%)
头部命中率（假设 8× 富集）        4%    → 20 个真阳性
────────────────────────────────────────────────
实验量 ↓ 20×   │   召回 40%（丢掉 30 个真阳性）
```

划不划算完全取决于**一个 hit 的边际价值 vs 一个孔的边际成本**。靶点富集的小库不值得
做预筛；十万级 DEL 或表型库、或者动物实验前的排序，杠杆才起来。

### 6.1 评价协议：不做这五件事，数字不可信

1. **基线阶梯必须齐。** 训练集平均位移 / 最近邻检索 / 线性加性 / oracle 上界。缺 oracle
   就区分不了"先验饱和"和"先验太弱"（§3.10）。
2. **分层报告。** 未见靶点、未见细胞系、未见组合是三种难度。不要拿容易的划分当难的证据。
3. **只在 DE 基因上评，且评方向和幅度。** 全转录组相关系数被未扰动基因主导，空模型也能
   看着很漂亮。
4. **报噪声天花板的百分比**，而不是裸分数。
5. **全记分卡打分。** 只优化一个通道很容易在合计上净为负（§3.9）。

**两个必查的技术细节。** 划分数据集用稳定摘要（`zlib.crc32`）而不是 Python 的 `hash()`
——后者每进程加盐，划分下个 session 就复现不出来了。做 DE 显著性指标时提交**细胞级**
数据而非重复的均值，否则组内方差为零会扭曲秩和检验，而且扭曲方向依赖模型质量——弱模型
反而可能因此得分变高。

---

## 7. 三阶段路线

每个阶段都带一个**停止条件**。虚拟细胞项目最常见的失败不是做不出来，是没定义什么情况下
该判定此路不通。

### Phase 0 — 盘点与地基（2–4 周，零 GPU）

- 建 K562 与 RPE1 的 **M0 状态卡**，字段对齐 CCLE/ENCODE 当期 release
- 在该系 Perturb-seq 上跑完整**基线阶梯**，算出**噪声天花板**（重复孔之间的一致性上限）。
  这一步产出的表比后面任何模型都重要
- 对准备用的任何预训练模型做 **accession 集合求交的污染审计**，并把判定原文
  （"预训练污染、标签干净"）写进模板
- 把化合物侧的 **L1000 检索基线**和**结构指纹最近邻**也建好——它们是 M3 必须打赢的对象

> **停止条件** 如果噪声天花板本身就低到基线已经贴顶，说明这条系的这个读出没有可建模的
> 余量，换读出或换系，不要硬训。

### Phase 1 — 两条主力模块（1–2 月，少量 GPU）

- **M2 系内扰动模型**（共识 + 靶点表征残差），分层报未见靶点 / 已见靶点
- **M3 化合物-表型检索**，在留出化合物上报 MoA top-k 富集倍数，与结构指纹基线并排
- 顺式侧接 **M1 闸门**：对候选编辑先判"在这条系里能不能真的关掉"，带 ±40bp 位置对照
- 产出第一版**富集倍数 → 实验减量**的对账表（§6 那个算式，填真实数字）

> **停止条件** M2 打不赢"训练集平均位移"，或 M3 打不赢结构指纹最近邻——停下来改架构，
> 不要靠调参硬凑，也不要换个指标重报。

### Phase 2 — 整合与路由（2–3 月）

- **顺式与反式并列呈现**（不串联），每个答案带自己的有效域声明
- 接 **M6**：把 M2 的通路位移喂进 `grn_pipeline` 的临界慢化引擎，输出"这条通路离开关点
  还有多远"
- 建 **M7 路由器**：四值判定 + 数值审计（工具返回里没有的数字一律标 `[UNVERIFIED]`）
- 加 **M4 形态层**（U2OS/JUMP），源分布必须是同批次对照孔而不是噪声，否则就退化成一个
  普通的条件生成模型

> **停止条件** 路由器的 `not_representable` 召回率上不去（该说不知道的时候在硬答），
> 这个层就不能上线——宁可不要路由器，也不要一个什么都敢答的。

### 不在一期的东西，以及原因

跨细胞系泛化（§3.1 架构受限，等低秩分解方案成型）；CRISPRa/过表达（§3.2 无可用训练
模态）；绝对 IC50（只做排序）；iPSC 分化体系与原代细胞（公开扰动数据太薄）；空间/组织层
（和"细胞系筛选"这个目标不在一条线上）。

---

## 8. 与现有仓库的衔接

`grn_pipeline` 已经是这张地图上的 **L6 层**，而且是完成度最高的一层——不是要新写，
是要接上去：

| 现有模块 | 已有能力 | 在虚拟细胞里的新角色 |
|---|---|---|
| M1 / M11 对称与纤维化 | Aut(G) 商网络；27→11 纤维 | 把 M2 输出的基因级位移**压到不可约核心**再解释，避免把旁系同源冗余当成独立信号 |
| M2 / M12 CRNT 亏格 | δ=0 单稳 / δ≥1 可开关 | 先判定一条通路**拓扑上是否可能有开关**，再决定值不值得建动力学模型 |
| M15 / M16 Markevich + DNB | 双稳窗口 [39.25, 57.38] nM；λ→0、τ≈4720 s | 虚拟细胞的**状态稳定性读出**：这条通路离开关点多远 |
| M18 滴定阳性对照 | 方差与自相关在阈值处峰值 ≈5× | M6 的**灵敏度证明**——证明这个读出不是瞎的，是这一层能上线的前提 |
| M14 18 通路图谱 | 每条通路的纤维压缩比 + 生物标志类别 | 虚拟细胞的**通路路由表**：一个扰动落在哪条通路、该用哪类读出 |
| M10 打分校验 | opt_score ρ=+0.6；binding_confidence ρ=−0.2 | M5/结合排序模块的**指标选择依据**（§5 已引用） |
| M9 占位率标定 | 98% 占位 → 网络接近临界点 | 把化合物剂量接到网络稳定性上——L5 与 L6 之间目前唯一走通的桥 |

换句话说，仓库里已经有的是**机理骨架加一套诚实性纪律**（每个数字都是算出来的、每条结论
都带严格性标签）。虚拟细胞要补的是数据驱动的 L0–L5，而那套纪律恰好就是 §6 评价协议想要
的东西——不用重新发明。

---

## 关于文中数字

- §3 的 12 条全部来自之前几轮在真实数据（Replogle/Nadig CRISPRi、Norman 2019 CRISPRa、
  六细胞系语料 236,606 细胞）上的实测，可复算。
- §2 的数据规模是量级参考，正式立项前应对当期 release 逐项核对。
- §1 的模型清单是按方法族梳理的路由表，不是穷举，也不含性能排名——"哪个最好"要去看独立
  benchmark（scIB、BEELINE 及各模态的评测），不要看方法论文的自报成绩。
