# Copy this file to `.env` and fill in your real credentials.
# `.env` is gitignored so secrets are never committed.

# Modal (compute backend)
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=

# Google DeepMind AlphaGenome API
# Request a key at https://deepmind.google.com/science/alphagenome
ALPHAGENOME_API_KEY=
